=== MUTATIONS ===
- [CAUGHT] 1. Delete the prod edge approval for record_version 4 (edge:payments@payments/prod<-cluster@platform/prod.cluster_endpoint, fact f135) and strip its id from the
    how: v_edge_approval strictly filters by exact version (views.sql:288-294), so after deletion only the v3 row (fact f069) remains for this edge and v_uptake_term's approved-term subquery (`a.version = r.version`, views.sql:766-768) finds nothing for version 4 — the v3 approval does not satisfy the v4 gate. v_uptake_gate for (edge, version=4) flips to n_unmet=1, passes=0, awaiting='approval: payments-oncall'. v_uptake_audit_flag for that decision (still recorded, still record_version=4) now reports flag='unmet at decision time: approval: payments-oncall'. This is also a pinned check already in the repo: scenarios/multistack/scenario.py:180-187 `m_no_edge_approval` performs exactly this mutation an
- [PARTIAL] 2. Change the payments/prod hyper-preview's against_record.version from 4 to 3 (fact f132, plan.summarized subject stage:payments/prod, producer cluster@platfor
    how: The prod edge's gate is NOT affected the way the task's hypothesis suggested: payments/prod's binding terms are `not_held` + `approved` only (fact f022) — it never declares a `plan_safe_or_approved` term, so v_uptake_gate for the prod edge at v4 is untouched (still n_unmet=0, passes=1) regardless of whether a matching preview exists. What DOES change: v_pending_uptake's display-only preview columns for the prod row (preview_fact/preview_note/preview_safe, from the LEFT JOIN v_record_plan at views.sql:868) silently go to NULL, because v_record_plan for producer='cluster@platform/prod' now only has a version=3 row. The staging edge is completely unaffected — its producer key is the distinct st
- [ESCAPED] 3. The cluster leg of P12 in platform/prod (transition:P12-cluster-prod, fact f126) cites record_version 1 (the stale/old network record) instead of 2, i.e. the
    how: transition.started's `record_version` field is surfaced by v_transition (views.sql:168) purely as a display column — it is never joined against v_record, v_uptaken, or any edge-consistency check anywhere in views.sql or scenarios/multistack/scenario.py. Grepping for 'record_version' across render.py and scenario.py shows only edge-approval/uptake.decided usages (scenario.py:169,181,182,190,219); none reference transition.started's record_version. Nothing in the schema or the audit views cross-checks that the record a stack's enactment claims to consume matches the producer's actual published/uptaken record at that time.
- [ESCAPED] 4. Add a binding.declared instance edge from payments@payments/prod back to network@platform/prod (edge:network@platform/prod<-payments@payments/prod.some_key, 
    how: v_impact (views.sql:919-936) is a recursive CTE guarded only by `d.depth < 8 AND instr(d.path, e.consumer) = 0` — it terminates (does not hang or error) by silently refusing to revisit a node already in the path, but nothing detects or surfaces that a cycle exists; the walk from root=network@platform/prod simply stops one hop short of looping back. This is a pre-existing, explicitly declared boundary, not a new finding: smells.md #28 states verbatim 'The impact walk guards against cycles by not revisiting a node; it does not flag the cycle... no view does it yet', and README.md:314 repeats it ('a cycle in declared edges only stops the impact walk, it is not flagged').
- [CAUGHT] 1. Delete f077 (bot uptake.decided for base_image_version=41 on edge:payments<-platform-images.base_image_version@*) while A231's config (f078) still pins base_
    how: Not caught by lint (render.lint returns []) or by any dedicated audit view — v_uptake_audit_flag / v_audit_flag never fire for by-version edges (they only check `d.kind='uptake.decided'` gate terms, and this edge has no `terms`). The inconsistency surfaces only as a *value* discrepancy: SELECT * FROM v_pin_uptake now returns pinned_version=40, pinned_by='bot:renovate' (f026, the v40 PR), pending=1 — i.e. the by-version screen says v41 is still awaiting a bot decision even though A231 (which the same row's pinned_in still names) already carries it. SELECT stage,pin_state,carried_pin FROM v_pin_stage still shows payments/dev and payments/staging as pin_state='current', carried_pin=41 — so the 
- [CAUGHT] 2. Add freight A232 (discovered Wed 08:00 on warehouse:payments) whose config pins base_image_version=40 (a downgrade from 41) and roll it to payments/dev.
    how: v_pin_stage now shows payments/dev with pin_state='behind', carried_pin=40, carried='A232' (published_version still 41), i.e. exactly the 'behind' state the mutation asked about. v_pin_uptake.pinned_in is unchanged at 'A231' (pending stays 0, pinned_version stays 41) — by design pinned_in/pinned_version report the fact that first pinned v41 (evidence), not what any given stage currently carries; per-stage truth lives in v_pin_stage, which does update correctly. The page's uptake screen would show the pin-set's top summary cell 'pinned v41 ... in freight A231' next to a per-stage payments/dev cell reading 'pin v40 · behind' — adjacent columns disambiguate rather than contradict. A side effect
- [CAUGHT] 3. Name a never-discovered freight P13 as the platform member of the pin-set.
    how: render.lint rejects it directly: ['f093 (line 93): pin-set member platform: P13 is not a discovered freight'] — the lint rule at render.py's release.pinned block (`for prog, fr in members: if fr not in declared['freight']: err(...)`) fires. With P13 subsequently discovered (freight.discovered added before f093, no promotions), lint is clean and v_pinset_status behaves correctly: platform contributes 0 carried at every environment (P13 never carries anywhere: v_pinset_env rows for platform all show carried_now=0, cell='none'), so dev/staging read 'partial' (1/2, from payments' A231 still carrying) and prod reads 'pending' (0/2) — no crash, no false 'complete'. self_checks (58 total) fails 4 c
- [PARTIAL] 4. Remove the release.pinned fact (f093) entirely.
    how: The page renders cleanly without the pin-set screen: v_pinset returns [] and screen_pinset(db, as_of) returns '' (its own `if not pinsets: return ""` guard), so render_page concatenates fine — full page built successfully (428421 chars), with no 'id="pinset"' anchor or 'composite freight' heading anywhere in the output. Lint is clean (no fact refs f093, so removing it and stripping refs elsewhere leaves no dangling ref errors). But self_checks do NOT cleanly split into 'mentions pin-set' vs 'passes' — of 58 checks, 4 fail: two explicitly name the pin-set ('pin-set: k8s 1.31 complete...', 'as of Wed 09:00: everything converged... k8s 1.31 complete everywhere...'), but two others whose *names*
- [CAUGHT] 1. Swap P12's network/cluster transition.finished timestamps in platform/prod (f123<->f130), keeping refs intact.
    how: lint fires 'f130 (line 123): transition.finished before transition.started' (the naive ts-only swap leaves cluster finishing at 14:20 while it started at 14:22); independently, two scenario self-checks tied to the between-legs snapshots fail: the 14:21:15 check expecting 'network at P12, cluster at P11' and the 14:23 check expecting 'in-flight on the cluster leg, phase control-plane', because the swap moves network's own finish to 14:50 and cluster's to 14:20, contradicting those snapshots' narrative. Separately (not a failure, an answer to the question): v_first_carried for (platform/prod, P12) stays at='2026-09-01T14:50:00Z' — unchanged from baseline — because it is max(per-stack first_at)
- [ESCAPED] 2. Add a platform/staging soak verification.recorded (check='soak', outcome=pass) at 10:50, between the failed cluster leg (finished 10:25) and the successful r
    how: At the fixture's primary/end instants nothing fires: self_checks 58/58 pass and lint is empty, because v_verified (views.sql ~ line 260s) already dedups to one row per (stage,freight,chk) by latest seq, and the genuine post-carry soak verification at 12:20 (f119, higher seq) always wins over the injected 10:50 fact — the premature record is silently discarded before v_gate_term ever evaluates it, so the 'recorded before carried — re-run' unmet-term path (views.sql:460-462) never gets exercised in the full fixture. Isolating the mechanism (as_of 2026-09-01T11:10:00Z, i.e. before the genuine 11:20/12:20 verifications exist) confirms the rule IS correctly enforced when it's the only record: sat
- [ESCAPED] 3. Add a second (uptake-only) transition on payments/prod's payments stack, freight NULL, record_version 4, at 16:00 — the pulumi-service by-reference shape, bu
    how: No self-check or lint fires (58/58 pass, lint empty) at any snapshot instant the scenario tests. But between the injected transition (16:02) and the next real freight promotion (A231 at 16:35), v_carried_stack.since — and therefore v_carried.since and v_grid.carried_since / last_outcome_freight — is corrupted: SQLite's `IS NOT` treats a NULL freight as 'not equal to' the carried freight, so the injected NULL-freight enactment counts as 'a success of another freight' in views.sql:206-210's window computation, pushing the 'since' threshold past A230's own finish and making the whole subquery return NULL. carried stays correctly 'A230', but carried_since silently drops to NULL and last_outcome_
- [CAUGHT] 4. Give the retried leg P12-cluster-staging-2 (f110) a ref to a new promotion.decided by user:rafael at 10:44 (rationale supplied) instead of the policy decisio
    how: The audit self-check fails outright: 'audit: no promotion and no uptake decision is flagged' — v_audit_flag (views.sql:1063-1069) correctly flags the new decision: actor 'user:rafael' doesn't match 'policy:%', producing flag 'decided by user:rafael directly, not by the stage policy'. Surprising but non-alarming side effect: v_grid for platform/staging silently adopts the rogue fact as the stage's decision-of-record — decided_at flips to '2026-09-01T10:44:00Z' and decided_by to 'user:rafael' (v_grid just takes the latest promotion.decided by ts, views.sql:156, with no actor filter), even though carried/status/lanes stay correct (still converged on P12, lane cell still 'reached' at 11:05). So 

=== LENS sql-correctness ===
- (high) [CONFIRMED] `--pure` now fails on pulumi-service itself, contradicting the doc claim it renders byte-identical there @ views.sql:685 (v_trace_summary); README.md "`--pure` keeps them as views: on pulumi-service it renders the same page byte for byte"; smells.md #18 "On pulumi-service the pages are byte-identical"
    evidence: `python3 render.py --pure` (pulumi-service, HEAD of this worktree) exits 1: `self-check: 3/60 failed`, all three raising `OperationalError: too many references to "facts": max 65535` in the trace checks. Root-caused by bisecting views.sql against the parent commit (06f963b24832, before the multistack session's warehouse-key generalization): loading that older views.sql against the same pulumi-service facts.jsonl, `--pure` succeeds and returns 10 rows from v_trace_summary; loading the current views.sql, `--pure` throws the same reference-count error even for pulumi-service. The added `CASE WHEN
    suggestion: Either materialise v_trace/v_trace_cell as an explicit intermediate step before v_trace_summary's EXISTS checks (breaking the inlining chain), or replace the two EXISTS/NOT EXISTS subqueries with a single scalar lookup against a smaller, already-filtered source (e.g. reuse v_membership/v_freight's `cut_at` column via a narrower CTE) so the reference count stays under the ceiling. Also correct the 
    verdict: Headline reproduces exactly at HEAD (working copy clean per `jj status`; views.sql last touched in 15b5d9d9). `python3 render.py --pure` (default scenario pulumi-service) exits 1 with `self-check: 3/60 failed`, all three trace checks raising `OperationalError: too many references to "facts": max 65535` (log: scratchpad/verify3/sql-correctness/pure-ps.log). The docs assert the opposite: README.md:35 "renders the same page byte for byte" and smells.md:186 "On pulumi-service the pages are byte-identical". Bisect confirmed via harness f1_pure.py (render.PURE=True, pulumi-service facts, as_of 2026-
- (high) [CONFIRMED] v_inflight collapses a stage's per-stack transitions to one row, silently dropping a concurrently-open leg @ views.sql:249-253 (v_inflight), also views.sql:255-259 (v_last_finished, same shape)
    evidence: v_inflight is `SELECT t.* FROM v_freight_transition t WHERE t.finished_at IS NULL AND t.started_seq = (SELECT max(u.started_seq) FROM v_freight_transition u WHERE u.stage=t.stage AND u.finished_at IS NULL)` — one row per STAGE, picked by latest start, even though v_freight_transition (new this session) is keyed per (stage, stack). Mutation: on the multistack fixture, drop the `transition.finished` fact for `transition:P12-network-prod` (leaving that stack's transition open) and re-render at 2026-09-01T14:23:30Z, the instant the fixture already uses to test the cluster leg's in-flight state. At
    suggestion: Either key v_inflight/v_last_finished by (stage, stack) and have v_grid render a per-stack detail (mirroring what v_carried_stack/v_carried already did for the 'carried' side, per smells.md #4/#18), or explicitly aggregate multiple concurrently-open stack transitions into one row with a count/list rather than silently picking the single latest-started one.
    verdict: Reproduced with harness f2_5.py: multistack facts, remove `transition.finished` for `transition:P12-network-prod`, as_of 2026-09-01T14:23:30Z. `SELECT ... FROM v_freight_transition WHERE stage='platform/prod' AND finished_at IS NULL` returns 2 rows (P12-cluster-prod started_seq 125, P12-network-prod started_seq 122); `SELECT ... FROM v_inflight WHERE stage='platform/prod'` returns 1 row (cluster only); v_grid platform/prod = {status:'in-flight', carried:'P11', stacks_detail:'cluster@P11, network@P11', inflight:'transition:P12-cluster-prod', inflight_freight:'P12', last_phase:'control-plane'} —
- (low) [CONFIRMED] Dead tautological predicate in v_estate's pending_downstream subquery @ views.sql:987-989
    evidence: `(SELECT count(*) FROM v_pending_uptake pu WHERE pu.producer = pu.producer AND substr(pu.producer, instr(pu.producer,'@')+1) = g.stage AND pu.pending = 1) AS pending_downstream` — the clause `pu.producer = pu.producer` is always true (or NULL, filtering nothing pu.producer can't be given the schema), so it does no work; the actual filtering comes entirely from the substr comparison. Self-check 'estate: ... platform/prod has one pending downstream' currently passes, so results are not wrong today, but the predicate reads as if it constrains something and doesn't.
    suggestion: Remove the dead clause, or if it was meant to express something else (e.g. restricting to producers on this program), replace it with the intended condition.
    verdict: views.sql:987-989 contains `WHERE pu.producer = pu.producer AND substr(pu.producer, instr(pu.producer,'@')+1) = g.stage AND pu.pending = 1`. Harness f2_5.py at 2026-09-01T15:30:00Z: `SELECT count(*) FROM v_pending_uptake WHERE producer IS NULL` = 0; the subquery evaluated with and without the clause gives identical rows for all six stages ([platform/dev 0, platform/staging 0, platform/prod 1, payments/dev 0, payments/staging 0, payments/prod 0]) and matches `SELECT stage, pending_downstream FROM v_estate`. Even with a NULL producer the substr comparison would already yield NULL, so the clause 
- (low) [CONFIRMED] v_carried's tie-break for 'last' uses the fact-id string, not seq, to order same-timestamp finishes @ views.sql:244-246
    evidence: `JOIN v_carried_stack last ON last.stage=s.stage AND last.transition = (SELECT k3.transition FROM v_carried_stack k3 WHERE k3.stage=s.stage ORDER BY k3.since DESC, k3.fact DESC LIMIT 1)` — the secondary sort key is `k3.fact` (the fact id string), not `finished_seq`/`seq`. Every other 'latest by arrival' view in this file (v_warehouse, v_stage, v_policy, v_freight, v_approval, v_edge_approval, ...) breaks ties by `seq`, per the file's own header comment ("never by `ts` alone: ... the ledger's order is the tie-break"). Two stacks whose successful transitions finish at the identical timestamp wou
    suggestion: Swap `k3.fact DESC` for the underlying transition's `finished_seq DESC` (already selected on v_carried_stack via the join to v_freight_transition) to match the file's own stated tie-break discipline.
    verdict: Code claim exact: views.sql:245-246 `ORDER BY k3.since DESC, k3.fact DESC LIMIT 1`, against the header's discipline at views.sql:8-9 ("'Latest' is always by `seq` ... never by `ts` alone"). Divergence demonstrated with a mutation in f2_5.py: set P12-network-prod's finish ts equal to P12-cluster-prod's (14:50:00Z) and rename the cluster finish fact id to 'a999' (cluster stays later in the file: seq 130 vs 129). v_carried platform/prod then returns transition:P12-network-prod / ops_update 1012 / fact f123, while the seq-order tie-break (`JOIN facts f ON f.id = k.fact ORDER BY k.since DESC, f.seq
- (low) [DOWNGRADED] v_uptake_audit_flag's group_concat over unmet/evidence terms has no ORDER BY on its source subquery @ views.sql:1111-1112, 1116
    evidence: `(SELECT group_concat(x.requirement, '; ') FROM v_uptake_audit_term x WHERE x.decision=d.id AND x.satisfied_by IS NULL) AS unmet` has no ORDER BY inside the subquery — the same shape smells.md #18 says was found and fixed elsewhere this session (v_carried's stacks_detail, v_lanes_base's partial_detail, v_estate's wired — all now `ORDER BY ... k2.stack`/`pu.key`). I mutated an edge's terms to carry two simultaneously-unmet 'approved' terms and compared materialised vs `--pure` output for v_uptake_audit_flag: the two runs agreed (both returned 'approval: roleZ; approval: roleA' in idx order), so
    suggestion: Add `ORDER BY x.idx` inside both subqueries for consistency with the fix applied to the sibling views this same session, even though no divergence was reproduced.
    verdict: The absence of ORDER BY at views.sql:1111-1112 and 1116 is real, but the finding self-reports no reproduced divergence and the fixture cannot produce one: `SELECT decision, count(*), sum(satisfied_by IS NULL) FROM v_uptake_audit_term GROUP BY decision` at 2026-09-01T15:30:00Z gives f052/f070/f116 each with n_terms=1, n_unmet=0, so every group_concat runs over at most one row; `SELECT fact, unmet, evidence, flag FROM v_uptake_audit_flag` shows unmet NULL everywhere and single-fact evidence (f069, f050, f115). The 'inconsistent with sibling views fixed this session' framing is also wrong: the di
  covered well:
    · v_grid row count equals v_stage row count at every one of 135 distinct multistack timestamps and 131 distinct pulumi-service timestamps (full sweep, 0 mismatches) — no fan-out into the grid.
    · v_lanes row count is exactly n_stages × n_freight (24 = 6×4 on multistack at the primary instant) with no fan-out, because v_gate_eval (joined by stage only in v_lanes_base) is itself uniquely keyed per stage (v_candidate has exactly one row per stage, so the g.freight=c.freight join in v_gate_eval 
    · Materialised vs `--pure` agree, set-for-set, on all 62 views at the scenario's own primary instant (scn.DEFAULT_AS_OF) on multistack except v_trace_summary, which errors outright under --pure (see finding). Confirms the earlier apparent v_release_prs/v_pinset_env 'diffs' from a first pass were artif
    · All scalar subqueries in v_gate_term/v_uptake_term that lack an explicit LIMIT 1 (verified, carried, not_held, plan_safe_or_approved's plan half) are safe by construction because their source views (v_verified, v_carried, v_plan) already dedup to at most one row per the relevant key; the ones that c
    · v_pin_uptake / v_pin_stage / v_impact are NULL-safe when a by-version producer has never published: constructed this by removing all output.published facts for platform-images and re-rendering — pending comes back NULL (not a false 0/1), pin_state comes back 'nothing' for every consuming stage, and 
    · Pattern-vs-instance edge exclusion is correct: v_edge_instance's `WHERE e.role='instance'` combined with pattern rows never carrying `terms` (so json_each on NULL yields no rows) keeps pattern-level bindings out of v_edge_term/v_uptake_term/v_pending_uptake entirely — confirmed by the existing self-
    · v_impact's recursive CTE correctly produces two independent rows for `payments@payments/prod` when queried from two different roots (network@platform/prod via 2 hops, platform-images directly) rather than merging them — this is the intended diamond-dependency behavior, not double counting within one
    · 'Latest as of the clock' views need no explicit per-row clock filter because build_db only ever inserts facts with `ts <= as_of`; the clock table is read only by hold-expiry logic, which is the one place 'active' is genuinely relative to now.
    · v_carried_stack's `since` subquery is stack-scoped and self-consistent for any stack count: because the surrounding v_carried_stack row is itself defined as the MAX(finished_seq) success for that (stage,stack), the coalesce baseline (max finished_seq of a *different* freight) is always strictly less
    · v_pinset_env / v_pinset_status produce exactly one row per (program, environment) with no fan-out, since a program has at most one stage per environment.

=== LENS ledger-purity ===
- (medium) [CONFIRMED] --pure mode crashes on trace queries (violates the documented 'must render the same page' invariant), pre-existing but undocumented @ render.py:190-193 (the invariant statement) via views.sql v_trace_summary/v_trace_cell/v_trace/v_lanes composition
    evidence: `python3 render.py --scenario multistack --pure --lint-only` → `self-check: 1/58 failed` — `trace: #812 is in platform/prod...` raises `OperationalError: too many references to "facts": max 65535`. Control run on the base scenario: `python3 render.py --pure --lint-only` → `self-check: 3/60 failed`, all three failures are trace checks (`#46181`, `#46173`, `#46120`) with the identical `OperationalError`. So this is pre-existing in pulumi-service, not introduced by the multistack change, but it is real and undocumented: render.py:190-193 states "--pure leaves [views] as views and must render the 
    suggestion: Either fix (the trace view chain — v_trace joins v_membership × v_freight × v_lanes, and v_lanes_base already cross-joins v_freight × v_stage; nesting that unmaterialised inside v_trace_cell_base's correlated subqueries multiplies reference count past SQLite's 64K limit) or, more cheaply, note the limitation in the `--pure` docstring/README so the flag's claim ("must render the same page") isn't s
    verdict: Reproduced in the worktree (sqlite 3.51.0): `python3 render.py --scenario multistack --pure --lint-only` -> `self-check: 1/58 failed` (trace: #812 ... raised OperationalError: too many references to "facts": max 65535); `python3 render.py --pure --lint-only` (pulumi-service) -> `self-check: 3/60 failed`, all three trace checks (#46181, #46173, #46120) with the same OperationalError. Two corrections to the finding's framing, both of which make the defect sharper, not weaker. (a) The multistack half is a declared boundary, not undocumented: README.md 'Run' section states "on multistack the trace
- (medium) [CONFIRMED] Shared by-version uptake blurb hardcodes a fixture-specific version number and mechanism, contradicting the row-derived discipline @ render.py:689-692 (screen_uptake, the by-version pin_html blurb)
    evidence: 'The uptake is that config change — here a bot PR that auto-merges on green checks — and from then on it is ordinary freight, meeting every stage's gates on the way. "Pending" means published but not pinned; "where is v41" is a lanes question.' — `grep -n "v41" render.py` → only this line. v41 is the multistack fixture's specific published AMI version (scenarios/multistack/fixture.py:436, `img41 = publish(...41...)`); the shared renderer bakes it into prose that ships on every scenario using by-version pins. The 'here a bot PR that auto-merges' clause is also stated unconditionally even though
    suggestion: Drop the literal v41 from the generic blurb (say 'where is the new version' or similar), and either derive the bot/auto-merge claim from pn['pinned_by']/pn['pinned_via'] per row, or soften the shared prose to not assert a specific mechanism that only holds for this scenario — matching the discipline already applied to section titles ('section titles that assert state are row-derived').
    verdict: `grep -n "renovate\|payments\|platform\|k8s\|1\.31\|AMI\|v41\|v13\|F41\|production-eu\|oncall" render.py` returns exactly one hit: render.py:692 `'"where is v41" is a lanes question.</p>'` — the shared renderer is otherwise free of fixture literals, so this is the lone deviation. v41 originates in scenarios/multistack/fixture.py:436-439 (`img41 = publish(... 41 ...)`, `u41 = uptake(... 41, "bot:renovate", ..., via="PR #4431 auto-merged on green checks", pr=4431)`). The blurb at render.py:689-692 asserts "here a bot PR that auto-merges on green checks" unconditionally, while the same section al
- (low) [CONFIRMED] Dead tautological predicate in v_estate.pending_downstream @ views.sql:988
    evidence: `(SELECT count(*) FROM v_pending_uptake pu WHERE pu.producer = pu.producer AND substr(pu.producer, instr(pu.producer, '@') + 1) = g.stage AND pu.pending = 1) AS pending_downstream` — the clause `pu.producer = pu.producer` is a self-comparison, always true for any non-NULL value, and every row of v_pending_uptake has a non-NULL producer (a required field on a by-reference edge instance). It contributes nothing to the filter; the real work is done entirely by the `substr(...) = g.stage` clause immediately after it. Self-check 'estate: ... platform/prod has one pending downstream' (scenario.py) p
    suggestion: Remove the dead clause. As written it reads like a copy-paste remnant of an intended condition (e.g. comparing against a specific edge or excluding NULLs) that was never filled in — harmless today, but confusing to a future reader and a spot where a real filter could have silently gone missing.
    verdict: Clause present verbatim at views.sql:988: `(SELECT count(*) FROM v_pending_uptake pu WHERE pu.producer = pu.producer AND substr(pu.producer, instr(pu.producer, '@') + 1) = g.stage AND pu.pending = 1) AS pending_downstream`. Scratch script (verify3/ledger-purity/mut2.py) evaluated the subquery with and without `pu.producer = pu.producer` against v_grid at six instants (15:30, 08:30, 10:30, 11:10, 14:21:15 Tue and Wed 09:00): `with==without: True` and `==v_estate: True` at every instant, e.g. rows at 15:30 [('platform/prod', 1), all others 0] and at 11:10 [('platform/staging', 1), all others 0];
- (low) [DOWNGRADED] Snapshot captions: everything but the '1.31'/'k8s 1.31' label is directly pinned; the label itself rests on unverified fact-payload text @ scenarios/multistack/scenario.py:25-32 (SNAPSHOTS) vs. self_checks
    evidence: Full caption-by-check mapping: B (10:30, cluster leg failed/quota, network at P12) is pinned by the check at line 132-136 ('ResourceLimitExceeded' in r['last_error'], stacks_detail == 'cluster@P11, network@P12'). C (11:10, preview not safe/uptake waits) is pinned by the check at line 138-141 (awaiting == 'approval: payments-oncall (preview not safe)', term_outcome == 'open'). D (14:21, network at P12/cluster at P11) is pinned by the check at line 143-148 (status=='partial', stacks_detail=='cluster@P11, network@P12'). E (Wed 09:00, everything converged) is pinned by the check at line 152-158 (v
    suggestion: Either add a one-line self-check pinning v_pinset.display == 'k8s 1.31' AND json_extract(members,'$.platform')=='P12' AND json_extract(members,'$.payments')=='A231', or treat this as acceptable (the display string is raw ledger payload text rendered as-is, not a computed/derived state, so it may be intentionally out of scope for the 'derived state must be pinned' discipline) — but the gap is real 
    verdict: The label half holds; the members-mapping half is refuted by mutation. Baseline `render.self_checks(scn, facts)` = 58/58. Mutating the release.pinned fact in memory via render.mutate (scratch mut2.py): `payload.display -> 'k8s 1.32'` -> 58/58 passed; `display` key deleted -> 58/58 passed; `subject -> release:k8s-1.32` -> 58/58 passed. So the 'k8s 1.31' text used in SNAPSHOTS captions A and E (scenario.py:28,31), the check names at scenario.py:115,152, and PAGE['thesis'] is unpinned by any self-check, despite the comment at scenario.py:26 ("every claim is pinned by a self-check at the same inst
  covered well:
    · 58/58 self-checks pass on a clean `python3 render.py --scenario multistack` run (materialised mode); 60/60 still pass on the pulumi-service scenario, so the shared views.sql change introduced no regression there.
    · No 'None', Python repr, or unescaped value leaks anywhere in out/multistack/index.html: `grep -c "None"` → 0 hits; the only `&#x27;` occurrences are inside the escaped CREATE VIEW SQL panel text (e.g. 'superseded'), correctly HTML-escaped by esc().
    · No stage/freight/program name is hardcoded in render.py: `grep -n "platform\|payments\|'P12'\|A231\|A230" render.py` → zero matches; every screen (grid, lanes, trace, uptake, pinset, impact, estate, releases) pivots over programs(db) / query results.
    · The programs() pivot renders correctly for both teams: `<h3>platform</h3>` and `<h3>payments</h3>` each appear 10 times across the per-program screens (lanes, trace, releases, plus H3s elsewhere).
    · screen_diffgate correctly renders as empty (`id="diffgate"` has 0 occurrences in the page) because no multistack stage policy uses the plan_safe_or_approved term type (only used on the payments/staging edge, per fixture.py:217) — this is query-driven omission, not a bug; the diff-gate beat legitimat
    · screen_grid's if/elif ladder (render.py ~404-422) and screen_diffgate's dict switch (render.py ~584-589) both key off view-computed status/term_outcome columns, not re-derived Python logic — consistent with the 'switch on view-computed cell/status' discipline.
    · SQL panels shown on the page are the literal CREATE VIEW text: build_db captures VIEW_SQL from sqlite_master.sql before dropping/materialising each view (render.py:213-219), and the rendered <pre> for v_grid matches the views.sql source verbatim (confirmed by inspecting the grid section's SQL panel 
    · v_carried_stack / v_carried correctly model per-(stage,stack) partial carry state for the two-stack platform program, and the 'partial' status (between-legs D snapshot) renders with the correct stacks_detail — cross-checked against the D self-check.
    · v_lanes / v_trace correctly scope freight to their own program's stages via the warehouse→program join: self-check 'membership is per warehouse...' confirms platform PRs never appear in payments freight/lanes and vice versa, and a live grep of the lanes/trace HTML shows only the expected per-program
    · The mutation checks (edge/uptake-specific ones — wrong-role edge approval, missing edge approval, person-typed uptake, removed auto uptake, open cluster leg, unpinned freight, cross-program PR leak) all pass, confirming the views (not just the fixture) carry the by-reference and by-version uptake lo

=== LENS thesis-fidelity ===
- (high) [DOWNGRADED] The 'composite transition' that coordinates cutover ordering doesn't exist; the pin-set's order is inert prose @ thesis-architecture.md:391-396 vs render.py:753-754,784 and views.sql:969-978
    evidence: Architecture: "the composite transition coordinates cutover ordering and never overrides a member owner" (thesis-architecture.md:394-395) — naming a coordinating entity. The fixture has no such subject: `release.pinned`'s payload is `{display, members, order}` (fixture.py:459-462), and `order` ("platform", "payments") is read only to print "members in order: platform → payments" (render.py:753-754, 784) — never evaluated. Worse, `v_pinset_status`'s `min(ord)` (views.sql:970) is *not* the pin-set's declared order at all — it's `v_stage.ord` (views.sql:35, the stage's own dev/staging/prod ordina
    suggestion: Either add a real coordinating construct (a `transition:release:<name>` subject, or at minimum a view that flags a member shipped out of declared order) or soften the architecture sentence to say ordering is advisory/descriptive in the pin-set-as-query design this fixture settled on.
    verdict: Restates a declared boundary and a recorded smell. README.md 'Boundaries, on purpose' (final paragraph): "The pin-set declares an order for its members and nothing enforces it". smells.md #23 quotes the exact architecture sentence the finding cites: "nothing enforces the order or coordinates a cutover — the architecture's 'the composite transition coordinates cutover ordering' has no fact to stand on yet"; threads.md:59-62 lists "a fact for pin-set ordering / cutover coordination (smells #23)" as a post-felt-read candidate. The one novel sub-claim is accurate but inert: v_pinset_env takes `s.o
- (high) [DOWNGRADED] Composite freight is defined as member refs plus edge declarations, content-keyed; the fixture's pin-set has neither @ thesis-architecture.md:52-56 vs fixture.py:459-462
    evidence: Architecture: "a *pin-set intent fact*: a content-keyed set of member-freight references plus edge declarations — Merkle-shaped, so a release's key derives from its members' keys" (thesis-architecture.md:52-55). The fixture's `release.pinned` subject is the human-chosen `release:k8s-1.31` (not derived from P12/A231's keys), and its payload carries only freight refs (`members`, `order`) — no edge component, even though the k8s-1.31 story's actual coupling *is* an edge (E_CLUSTER's v4 uptake has to ship alongside P12/A231; nothing in the pin-set fact says so).
    suggestion: This is likely the honest resolution of a real tension the architecture glosses (see the (c) finding below on stage-local lineages) rather than a simple bug — but as written the doc's definition and the fixture's fact shape disagree on two structural points (identity, and the edges component). Either extend the pin-set payload with an edges/records component, or narrow the architecture's composite
    verdict: Literally true: fact f093 (facts.jsonl; fixture.py:459-462) is subject `release:k8s-1.31` with payload {display, members, order} and refs [f082, f078] (the two freight.discovered facts) — no content key, no edges component. But neither half is a pin-set-specific contradiction. (a) Identity: nothing in the fixture is hash-keyed — every subject is an alias (`freight:P12`, `freight:A231`, render.py:44 SUBJECT_TYPES, lint at render.py:110), so 'not derived from members' keys' is the fixture-wide convention, not a divergence peculiar to release.pinned. (b) The finding reads 'edge declarations' as t
- (medium) [CONFIRMED] 'Stack' is real grip-granularity content this scenario needed, but never becomes a subject @ thesis-architecture.md:33-51 (Subjects, P5) vs render.py:44 (SUBJECT_TYPES) and views.sql v_carried_stack
    evidence: P5 ("subjects must come in operable sizes... addressable for scoped operation, surgery, and blast-radius control") and the Subjects list name Stage, Freight, Release, Transition, Edge, Operation, Fleet dimensions — never Stack. `SUBJECT_TYPES = {"warehouse", "stage", "freight", "transition", "edge", "record", "release"}` (render.py:44) confirms stack is not one. It exists only as a JSON array string on `stage.declared`/`warehouse.declared` and a string field on transitions. `v_carried_stack` (views.sql:202) does deliver smells.md #4's per-stack grid cells, but by string-keying inside payloads 
    suggestion: Not necessarily a defect — flag as an open question the multistack scenario surfaces for P5: does a program with more than one freight-carrying artifact per stage need `stack` promoted to a subject, or is string-keyed sub-stage addressing sufficient? The architecture doesn't yet say.
    verdict: Reproduced, and no declared boundary or smell covers it. render.py:44 `SUBJECT_TYPES = {"warehouse", "stage", "freight", "transition", "edge", "record", "release"}` and lint at render.py:110 rejects any other subject type; hold.placed is keyed on `stage:` (views.sql:304 `WHERE f.kind = 'hold.placed'` joined on stage; program.md 'What writes what' table: `hold.placed · stage:<s>`), so nothing can hold or approve just the cluster leg of platform/prod. v_carried_stack (views.sql:203-215) reaches stack granularity by json_each over `warehouse.declared.stacks` (views.sql:30-32 v_program_stack) and 
- (medium) [REFUTED] Stage-local record lineages make a cross-environment pin-set structurally freight-only — the architecture's composite-freight sentence doesn't say this @ thesis-architecture.md:235-242 (record lineages stage-local) vs thesis-architecture.md:52-56 (composite = refs + edge declarations) vs fixture.py:214-221, 459-462
    evidence: The wiring section itself states "cross-stage value pinning is structurally impossible, as it should be" (thesis-architecture.md:239) because record lineages are stage-local. But the Subjects section's composite-freight definition says a release is member refs *plus edge declarations* — which would have to mean per-(stage, edge) record versions, since there's no single cross-stage edge value to pin. The fixture resolves this by leaving edges out of the pin-set entirely: E_CLUSTER's v4 uptake in each environment is decided independently, under its own gate, never referenced from `release.pinned
    suggestion: Add a paragraph to the architecture doc: a composite pins freight only; any record/edge state that must travel with it is necessarily per-stage and can only be referenced or approved per-environment, never embedded as a single value in the pin-set fact. This is a real design point the fixture discovered, not yet written down.
    verdict: The tension rests on reading 'edge declarations' (thesis-architecture.md:52-55) as per-(stage, edge) record versions. The cited source (log.md 2026-08-27-composite-freight-worked, ~lines 2012-2040) defines them as the binding *declarations* in the content-closed set — "Freight content = the closed set: member program artifacts + static config + binding declarations (edges as data). Resolved output values are per-stage occurrence products — evidence, never identity" — and then states verbatim the paragraph the finding asks to add: "cross-stage output pinning is impossible in principle — A's out
- (medium) [DOWNGRADED] The fixture is a concrete answer to OPEN call #1 (default uptake policy) that the doc hasn't credited @ thesis-architecture.md:259-269 (OPEN — the default uptake policy) vs fixture.py:214-221 (E_CLUSTER)
    evidence: OPEN 1 speculates: "Likely answer is per-stage defaults (auto below a line, gated above), but this wants customer evidence, not a priori choice" (thesis-architecture.md:267-269). The fixture builds exactly that ladder on one edge: dev=auto (no terms), staging=auto-if-safe (`plan_safe_or_approved`), prod=gated (`not_held`+`approved`) — fixture.py:216-221.
    suggestion: Cite this pattern in the OPEN call entry as a worked instance (still not customer evidence, but a concrete shape to point to).
    verdict: The ladder is real — fixture.py:216-221 E_CLUSTER: dev [] / staging [plan_safe_or_approved('payments-oncall')] / prod [not_held(), approved('payments-oncall')] — and thesis-architecture.md has no mention of the fixture (grep for fixture|delivery-ledger|multistack: none). Two reasons it is not a fidelity gap: (1) the credit is already queued — threads.md:38-40 "Datum for OPEN 1: … OPEN 1 stands as 'confirmed on this scenario', pending the multi-stack case" and threads.md:55-58 "the uptake dial per environment (auto / auto-if-safe / gated on the cross-team edge) is OPEN 1 rendered where it bites
- (low) [CONFIRMED] The edge-term vocabulary boundary (verified/carried undefined on an edge) is written in the views but never exercised @ views.sql:761-762 and 778/787 vs fixture.py (edge patterns) and scenario.py (self-checks/mutations)
    evidence: `v_uptake_term`'s comment says "Only the term types that make sense on an edge are defined here — `verified` and `carried` name stages and freight, which an uptake has neither of" (views.sql:761-762), and both CASE expressions fall through to `... || ' (not defined on an edge)'` / `... || ' is not defined on an edge'` (views.sql:778, 787). No edge pattern in fixture.py (E_SUBNETS, E_CLUSTER, E_IMAGE) ever declares a `verified` or `carried` term, and no self-check or mutation in scenario.py exercises those ELSE branches.
    suggestion: One mutation test — add a `verified` term to E_PROD's terms in memory and assert the gate/label surfaces "not defined on an edge" — would pin this boundary the same way the other 14 mutation checks pin mechanism, and it's exactly the vocabulary question OPEN call 4 (gate-expression language) needs answered at the edge site too.
    verdict: Reproduced. `grep -n -E "not defined|'verified'|verified\(" scenarios/multistack/scenario.py scenarios/pulumi-service/scenario.py` returns nothing; the 10 multistack mutation checks (scenario.py:177-246) cover verification timing, uptake audit, wrong role, safe preview, hold, missing auto uptake, open leg, unpinned freight, warehouse leak — none touches the ELSE branches at views.sql:778/787. The suggested mutation works as written: appending {type: verified, stage: payments/staging, check: integration} to f022 (E_PROD's terms) in memory and querying v_uptake_term at 15:30 returns idx 2 → labe
- (low) [DOWNGRADED] 'Carried since' vs 're-enacted at' is a real distinction this scenario forces that the architecture doesn't name @ scenario.py:58-61 vs thesis-architecture.md Promotion section (156-209)
    evidence: The self-check at scenario.py:58-61 pins that `v_carried_stack.carried_since` for payments/staging stays at the freight's first arrival (09:17) even though the stack was re-enacted at 11:40 to take up the cluster-record uptake (`last_enacted_at`). The architecture's Promotion section covers phase facts and supersession but never distinguishes "since when has this freight been carried" from "when did this stack last run" — a distinction that only shows up once a stack can be re-enacted for a reason unrelated to a new freight arriving (here: taking up an edge record).
    suggestion: Worth a line in the Promotion or grid section: a stack's `carried_since` is freight-arrival time, not last-enactment time; re-enactment for an edge uptake (or any other reason) doesn't reset it.
    verdict: Restates a recorded smell and an already-tracked doc addition. smells.md #19 'Carried-since is not last-enacted-at' records exactly this distinction ("`since` is now the first success of the current freight after the last success of any other; `last_enacted_at` is kept beside it. Two clocks, both honest, and the grid wanted the first"), implemented at views.sql:205-212 and pinned by scenario.py:58-61. threads.md:66-73 lists "carried-since ≠ last-enacted-at (#19)" under "Architecture-doc additions the multistack scenario owes". Accurate that thesis-architecture.md's Promotion section (156-209) 
- (low) [REFUTED] v_pin_uptake.pinned_in matches against the latest published version, not the actually-pinned version — untested where they'd diverge @ views.sql:889-892
    evidence: `pinned_in` is computed by matching `freight.config[key] = r.version` (the latest *published* record version), not `u.version` (the version the bot's PR actually pinned). In every snapshot the fixture exercises, these are equal by the time either matters, so no self-check catches a state where a newer version is published but not yet pinned while an older pin is still carried in some freight — the query would then attribute `pinned_in` to a freight that doesn't yet exist for the true pinned version, or omit it incorrectly.
    suggestion: Not a thesis-fidelity issue, and not currently wrong in any exercised snapshot — flag as an untested edge case rather than a bug; a snapshot with a newer publish outstanding against an older confirmed pin would exercise it.
    verdict: pinned_in is defined as the freight carrying the *published* version's pin, not the uptake-decided one: v_pin_stage's comment (views.sql:900-901) "the lane cell of the freight that carries the new pin", and pin_state's 'unpinned' branch (views.sql:909) is the vocabulary smells.md #24 declares ("published-but-not-pinned is the only edge-level state… the view says `unpinned` per stage while the edge says pinned"). The divergence window is already in the ledger, not untested: at as-of 2026-09-01T08:10 (f076 published v41, f026 pinned v40) v_pin_uptake returns published 41 / pinned 40 / pinned_in 
  covered well:
    · Uptake is never presumed, even when the policy is 'auto': every within-team and cross-team edge uptake writes an explicit `uptake.decided` fact by `policy:uptake` (fixture.py:349-351, 391, 468), and the mutation `m_no_auto_uptake` (scenario.py:218-226) proves the fact is load-bearing — removing it l
    · No cross-team decision is ever overridden by the other team or by the pin-set: payments-oncall (user:dana) grants both the edge approval and the stage approval independently under her own policy (fixture.py:490-493, 516-524), matching thesis-architecture.md:391-396's 'each downstream team's uptake i
    · Record lineages are honestly stage-local: E_CLUSTER is instantiated separately per environment with independent version numbering per producer stage (fixture.py:214-221, enacted at 391-524), matching thesis-architecture.md:235-242's 'cross-stage value pinning is structurally impossible'.
    · The hyper-preview construct smells.md #16 asked for is delivered as a real fact, not a string: `plan.summarized` against a proposed record (fixture.py:397, 488, 510), consumed by `v_record_plan`/`v_uptake_term` so the auto-if-safe term is evaluable at rest.
    · The by-version binding construct smells.md #17 asked for is delivered faithfully: `output.published` (bake) → `uptake.decided` by the bot's auto-merged PR → the pin riding inside `freight.discovered.config` → ordinary lanes (fixture.py:225-229, 436-442), distinct from the by-reference pair.
    · Membership and candidates never leak across programs: v_membership and v_candidate are keyed by warehouse, confirmed by self-check (scenario.py:68-73) and by the deliberate cross-program mutation check (scenario.py:244-249) that shows what breaks if the warehouse key is dropped.
    · 58/58 self-checks pass on a clean run of `python3 render.py --scenario multistack` (lint: 143 facts, clean; self-check: 58/58 passed), including 14 mutation checks that break a mechanism and confirm the views say so.

=== LENS fixture-coherence ===
- (high) [CONFIRMED] Binding `description` (the human-readable rationale for a binding's uptake policy) is silently dropped from the uptake screen in both scenarios @ render.py:607-652 (screen_uptake); baseline had it at render-before.py:594
    evidence: At the version pinned by jj commit tzrpklvq, render.py:594 rendered `state + f'<div class="small muted">{esc(r["description"])}</div>'` in screen_uptake. In the current render.py, screen_uptake (lines 607-652) never references row['description'] — the only `description` reference left in the whole file is the page <meta> tag at render.py:1100. The column is still produced by the view (views.sql:877, v_pending_uptake selects e.description), and the fixtures still author it: pulumi-service/fixture.py:153/156/159 ('An instance refresh drains the hot pool. Approve the trigger; never auto-uptake.',
    suggestion: Restore the description line in screen_uptake's per-edge card (and the by-version pin card, which never rendered it either) — the data is authored specifically to explain the trigger/approval mechanics of each edge and is otherwise invisible on the page.
    verdict: Reproduced. `jj diff --git --from tzrpklvq --to @ render.py` shows the exact hunk in screen_uptake: `-            state + f'<div class="small muted">{esc(r["description"])}</div>',` / `+            state + terms,` (baseline render.py:594 had the div). `grep -n description render.py` now returns only :1100 (page <meta>) and :1125 (argparse `description=__doc__`) — neither renders the column. views.sql:863 (v_pending_uptake) and :877 (v_pin_uptake) still select `e.description`, and both fixtures author it (pulumi-service/fixture.py:153,156,159; multistack/fixture.py:207,219,227). Rendered pages:
- (low) [DOWNGRADED] pulumi-service's gated AMI edge lost its ad-hoc preview string with nothing replacing it (expected consequence of resolving smells.md #16, but pulumi-service's fixture was never given the compensating fact) @ out/pulumi-service/index.html uptake screen (AMI edges); smells.md:121-126 vs :222-227
    evidence: Baseline (tzrpklvq): '...since Wed Aug 26 07:30 · 8h 45m · <a href="#" class="preview">preview workflow-pool@production with ami_id = ami-0f3c9e8b7a6d5c412</a>'. Current: '...since Wed Aug 26 07:30 · 8h 45m · a person decides' — the concrete preview hint is gone, replaced by nothing, because render.py now only shows a preview when a `plan.summarized` fact with `against_record` exists (the smells #16 fix), and pulumi-service's fixture.py never authors one for the pending v13 AMI record.
    suggestion: Not a bug in the mechanism — the old string-preview was itself the dishonest artifact smells.md #16 flags. Either accept the net information loss on pulumi-service's card as the correct outcome, or add the corresponding preview fact to pulumi-service/fixture.py so the resolved mechanism actually improves that scenario rather than only degrading it.
    verdict: The disappearance reproduces: the tzrpklvq tree rendered in scratch shows `since Wed Aug 26 07:30 · 8h 45m · <a href="#" class="preview">preview workflow-pool@production with ami_id = ami-0f3c9e8b7a6d5c412</a>` (baseline views.sql:695-696 built that string); current out/pulumi-service/index.html has `class="preview"` 0 times and `since Wed Aug 26 07:30 · 8h 45m · a person decides` instead; `grep against_record scenarios/pulumi-service/facts.jsonl` = 0 vs 3 in multistack. But (a) it restates a recorded, deliberately-scoped resolution: smells.md:121-126 (#16) itself says the honest artefact 'is 
- (low) [CONFIRMED] smells.md #16 reads as unresolved ('Not built') even though its resolution is documented three sections later as #22 @ smells.md:121-126 vs smells.md:222-227
    evidence: #16: 'Not built; it is the multi-stack scenario's first fact.' #22 (dated the same day, under 'the multistack scenario'): 'The hyper-preview is a fact now, and it needed its own lane... Stage plans and record previews are now two views over one kind, split on the presence of against_record.' README.md's multistack section also documents it as built ('The hyper-preview as a fact...').
    suggestion: Cross-reference or fold #16 into #22 so a reader of #16 alone isn't told the artifact is still unbuilt.
    verdict: Reproduced by the document's own convention. smells.md:7 states 'a smell that gets resolved says how', and #2 at :23 carries the '(resolved)' marker; `grep -n resolved smells.md` returns only :7 and :23. #16 at :125-126 ends 'Not built; it is the multi-stack scenario's first fact.' with no pointer, while #22 at :222-227 ('The hyper-preview is a fact now...') and README:106 ('The hyper-preview as a fact') record it built. Low, doc-only; a cross-reference cannot affect any self-check.
- (low) [DOWNGRADED] Several additive rendering changes also showed up in the pulumi-service diff — not bugs, but real content differences from the pinned baseline worth naming since the task asked for every remaining difference @ out/pulumi-service/index.html: new <section id="impact"> (render.py:794 screen_impact), new 'Uptake decisions' audit sub-table (render.py ~936-951), new 'preview against it' column in the uptake table (render.py:652), new fact-ref links on 'taken up by' lines (render.py:625)
    evidence: full.diff (before/after tag-split diff of the two rendered pages, SQL panels stripped) shows an entire new <section id="impact"> present 5 times in the current page and 0 times in the tzrpklvq render; a new 'Uptake decisions' table under the audit section citing facts f078/f050/f047/f036; a new 'preview against it' <th> and per-row cell; and fact-id links (e.g. <a class="fact" href="#f047">f047</a>) appended to 'taken up by' lines that had none before.
    suggestion: None needed — these are the shared-views generalization (impact analysis, uptake audit, hyper-preview column) correctly reaching pulumi-service's page as a side effect of one views.sql/render.py. Worth noting only because README currently frames 'impact as a queue' as a multistack addition (README.md multistack section) when it in fact now renders on both scenarios' pages.
    verdict: The facts reproduce: baseline (tzrpklvq rebuilt in scratch) out/pulumi-service/index.html has `<section id="impact"` 0, 'Uptake decisions' 0, 'preview against it' 0, and 'taken up by policy · Mon Aug 24 16:31<' with no fact link; current page has 5/5/5 and `taken up by policy · Mon Aug 24 16:31 <a class="fact" href="#f078">f078</a>` ×4 plus f036 ×1; the Uptake decisions table cites f036/f047/f050/f078 (render.py:794 screen_impact, :949 uptake audit, :652 'preview against it' header). But this is the declared design, not a finding: README:45 says views.sql is 'shared by every scenario', README:
  covered well:
    · pulumi-service regression count: `python3 render.py` → 'lint: 132 facts, clean' / 'self-check: 60/60 passed' (unchanged from baseline).
    · multistack self-checks: `python3 render.py --scenario multistack` → 'lint: 143 facts, clean' / 'self-check: 58/58 passed'.
    · Every ref points backward in time and no dangling ref ids: check.py over facts.jsonl found zero bad refs (`bad_refs == []`), zero duplicate ids, and ts is monotonic non-decreasing (append-only in time, matching fixture.py's own sort-before-emit).
    · Verification-postdates-carry, correctly modeled per multi-stack semantics: naive 'verify after every leg's most recent finish' check (check.py) flagged 3 false positives (f085, f094, f103) because it used max-finish-across-re-enactments; re-derived using the actual `v_first_carried` semantics (views
    · Uptake enactments carry the right record_version: check4.py cross-checked every transition.started with a record_version field against the latest uptake.decided version on the matching edge as of that timestamp, for both the within-team subnet edge and the cross-team cluster edge — zero mismatches a
    · PR numbers/authors/titles are consistent everywhere they're cited: check3.py found only expected overlaps (PR #4402/#4431 appear both as a freight-discovered PR entry authored 'renovate' and as the bot uptake's `pr` field with matching number) and no genuine title/author conflicts across all 8 disti
    · Spot-checked rationale accuracy against the facts they describe: f135's 'staging took the same replace cleanly at 11:31' checked against f117 (started 11:31:00, record_version 4) and f118 (finished 11:40:00, outcome succeeded, replace:1) — true. ap3's 'first prod cluster record; payments not deploye
    · Phase/error text plausibility for EKS/Pulumi: the ResourceLimitExceeded vCPU-quota error on aws:eks/nodeGroup:NodeGroup, and the control-plane → node-pool → retiring phase sequence with plausible detail text and timestamps that fit inside the enactment's own start/finish window, read as a coherent E
    · Numeric/narrative consistency in the pin-set story: P12's network summary (create=1) and 3-subnet output values (vs P11's 2) match the fixture docstring's 'adds a subnet'; the OIDC issuer strings rotate (D3V1→D3V2, STG1→STG2, PRD1→PRD2) consistent with 'rotating its OIDC issuer'; the cluster-record 
    · All five snapshot captions in scenario.py are pinned by a self-check at the same as_of instant (grid/awaiting-approval check for 15:30; failed-leg check for 10:30; not-safe-preview check for 11:10; partial-between-legs check for 14:21:15; fully-converged check for Wed 09:00) — no caption asserts som
    · No tautological self-checks found: every check queries a derived view (v_grid, v_carried_stack, v_lanes, v_membership, v_trace_summary, v_gate/v_gate_term, v_pending_uptake, v_uptake_term, v_edge/v_edge_instance, v_pin_uptake/v_pin_stage, v_pinset_status, v_estate, v_impact, v_audit_flag, v_uptake_a
    · Traced the by-version bot-uptake audit exemption to its mechanism: v_uptake_audit_flag's 'decided by X directly, not by the edge policy' branch (views.sql:1114) is gated on `e.terms IS NOT NULL`, which is false for the by-version pattern (its instance never carries a `terms` key) — so bot:renovate's
    · jj history check confirms tzrpklvq is the correct diff baseline the task named — the commit right before 'multistack scenario: two programs...' and 'generalize the views past one program and one stack' — and the working copy is clean (jj status: 'The working copy has no changes'), so the diff wasn't
