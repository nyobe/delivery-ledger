=== CONFIRMED ===
- (high) [sql-correctness] v_audit_decision/v_audit_flag blind to plan_safe_or_approved — production-eu can never be flagged for a missing approval @ views.sql:632
    evidence: views.sql:632 and :636 filter `t.type = 'approved'` only. production-eu's policy terms are `carried, verified, not_held, plan_safe_or_approved` (no `approved` term at all). Unmutated fixture query `SELECT * FROM v_audit_decision WHERE stage='production-eu'` returns both real decisions (f041 for F416, f086 for F417) with `required_role: None, approval_fact: None`. Consequence demo: appended a fabricated promotion.decided (policy-attributed, 1 ref) for production-eu/F418 while production [...]
    suggestion: Extend the required-approval lookup at views.sql:632/636 to also match `plan_safe_or_approved` terms, but gate the flag on plan safety at decision time too (join v_plan at ts<=decision.ts, safe=0) — otherwise a legitimately safe-plan auto-promotion gets wrongly flagged.
    verify: Confirmed, not refuted. v_audit_decision resolves required_role/approval_fact only through `t.type = 'approved'` (views.sql:632, :636), and production-eu's policy has no `approved` term — its approval requirement lives in `plan_safe_or_approved{role: oncall}`. So required_role is NULL for every production-eu decision and the 'gated stage [...]
- (medium) [sql-correctness] Audit's required_role lookup uses LIMIT 1 with no ORDER BY — a second required approval role is invisible @ views.sql:632
    evidence: Mutated production's policy to add a second `approved` term (role: sre) alongside the existing oncall term, without granting sre approval for F417. v_gate correctly shows `n_unmet: 1, awaiting: 'approval: sre'` (per-term evaluation is correct), but v_audit_decision's `required_role` (views.sql:632/636, LIMIT 1, no ORDER BY) stays pinned to 'oncall' — the sre requirement never surfaces in the audit at all.
    suggestion: Don't select one approval term via LIMIT 1; check that every approved/plan_safe_or_approved term in the policy has a matching approval fact, e.g. via NOT EXISTS over v_policy_term rather than a single scalar pick.
    verify: The finding reproduces exactly as described and is not a restated boundary or recorded smell. v_audit_decision (views.sql:632 and :636) selects a single `approved` term's role via a scalar subquery with LIMIT 1, so when a policy declares two `approved` terms, the audit checks only one of them; which one is decided by term authoring order [...]
- (medium) [sql-correctness] "recorded before X carried Y — re-run" fires even when the stage never carried the freight at all @ views.sql:375
    evidence: The '9999' coalesce sentinel at views.sql:372-374 (`coalesce((SELECT min(x.finished_at)...), '9999')`) makes "never carried" indistinguishable from "carried, but this verification predates it" for the message built at line 375. Mutation: added a pass verification for stage:staging/freight:F420/check:integration-tests, where staging has never carried F420 (F420 never left testing). Result: `v_gate_term` for production/F420 shows `unmet_text: 'verification: integration-tests in staging [...]
    suggestion: Only emit the 're-run' phrasing when a successful carry actually exists (EXISTS the x-subquery); otherwise fall through to generic 'verification: chk in stage' text, or borrow the carried term's own unmet phrasing ('staging to carry F420').
    verify: Reproduced with a concrete query. Adding a single pass verification for stage:staging / freight F420 / integration-tests (staging has zero transitions for F420; v_carried shows staging carrying F418) makes v_gate_term for (production, F420) emit unmet_text 'verification: integration-tests in staging (recorded before staging carried F420 — re- [...]
- (high) [ledger-purity] Status classification computed in Python (if/elif over raw columns) in three screens, not read as a view-computed column @ render.py:573
    evidence: screen_lanes, render.py:573-583: `if c["reached_at"]: ... elif c["inflight_since"]: cells.append(chip("in-flight", ...)) elif c["awaiting"]: cells.append(chip("awaiting") ...) elif c["last_outcome"] in ("failed", "abandoned"): cells.append(chip("failed" if c["last_outcome"] == "failed" else "superseded", ...)) else: cells.append('<span class="muted">—</span>')`. Identical chain in screen_trace, render.py:650-659. A third variant in screen_diffgate, render.py:690-696: `if r["term_at"] is [...]
    suggestion: Add a single computed classification column to v_lanes / v_trace_cell (and to the diffgate join) mirroring v_grid's status CASE, then have screen_lanes/screen_trace/screen_diffgate switch on that one column the way screen_grid and screen_uptake already do — eliminating the duplicated Python branching and making the classification checkable by [...]
    verify: The finding stands. (1) Factual claims verified at the cited lines: screen_lanes classifies a cell with an if/elif over raw v_lanes columns at render.py:573-580, screen_trace repeats the identical chain over v_trace_cell at 650-656, and screen_diffgate decides a 3-way term status from (term_at, safe) at 690-692; by contrast screen_grid [...]
- (high) [ledger-purity] screen_releases and v_releases hardcode specific stage names instead of pivoting over v_stage @ render.py:780
    evidence: render.py:780: `body = table(["release", "cut", "staging", "approved", "production", "production-eu"], rows)` — column headers are literal stage-name strings, not derived from a query. The backing view bakes the same three stage names into fixed subqueries: views.sql:608-618, e.g. `(SELECT l.reached_at FROM v_lanes l WHERE l.freight = fr.freight AND l.stage = 'staging') AS staging_at`, `... AND l.stage = 'production') AS production_at`, `... AND l.stage = 'production-eu') AS [...]
    suggestion: Pivot v_releases (and the releases table) over v_stage the same way v_lanes/screen_lanes already do, rather than one named subquery/column per stage — or, if the release-train screen is deliberately scoped to only the stages the release train visits, say so in smells.md the way the other fixture-specific compromises are documented.
    verify: The finding holds and is not a restatement of a declared boundary or an existing smell. Static receipts: render.py:780 is a literal header list; views.sql:612-618 pins stage names in four correlated subqueries (the finding counted three — the approval subqueries also pin `a.stage = 'production'`). Contrast receipts: v_stage returns five [...]
- (medium) [ledger-purity] Static prose blurb inside a SCREENS function names a stage and asserts a specific ledger state that goes stale on 4 of 5 rendered snapshot tabs @ render.py:731
    evidence: screen_uptake's blurb, render.py:729-732: "Each binding declares its uptake policy. The AMI bake published a new record and the worker pool's binding is gated, so the grid shows <em>available, gated</em> with a preview instead of an instance refresh nobody decided on. The image-reference binding is auto: the policy wrote the uptake decision the moment production published." This string is a fixed Python literal passed into `section(...)`; render_page (render.py:964-968) calls every SCREENS [...]
    suggestion: Derive the uptake blurb's specific claim from the query rows (e.g. only surface the "AMI bake published... shows available, gated" sentence when a v_pending_uptake row is actually pending for this as_of), or move that narrative out of the static blurb and into the per-row rendering the way screen_grid's per-row `detail` text already does.
    verify: The mechanism reproduces: screen_uptake's blurb (render.py:729-732) is a fixed Python literal passed to section(...), and render_page (render.py:964-968) calls every SCREENS function once per entry in SNAPSHOTS, so the sentence "the grid shows <em>available, gated</em> with a preview" renders verbatim under all five tabs. At the two Monday [...]
- (medium) [scenario-fidelity] README's self-check count is stale (51 vs. actual 54) @ /Users/claire/src/nyobe/delivery-ledger/README.md:105:None
    evidence: README.md:105 reads "51 self-checks pass, each pinning something a view must *include and exclude* at one of nine instants — and eight of them are **mutation checks**". Running `./render.py --lint-only` in the repo prints `self-check: 54/54 passed`. The mutation-check sub-count is accurate — render.py defines exactly 8 `mutation=` checks (lines 365, 372, 378, 387, 394, 401, 407, 412) — but the headline total has drifted from 51 to 54 without the README being updated.
    suggestion: Update README.md:105 to "54 self-checks" (or regenerate the count programmatically) so the discipline section matches what `render.py` actually asserts.
    verify: Confirmed. /Users/claire/src/nyobe/delivery-ledger/README.md:105 states "51 self-checks pass", while running `./render.py --lint-only` in that repo prints `self-check: 54/54 passed`. The mutation-check sub-count (eight) is accurate: `grep -c 'mutation=' render.py` returns 8. smells.md has no entry about the README count being stale (grep for [...]
- (medium) [scenario-fidelity] Production-eu parallelism claim is grounded but contradicts a primary source the README's own audience will check it against @ /Users/claire/src/pulumi/pulumi-service/doc/runbooks/reference/pr-and-weekly-release-process.md:42:None
    evidence: README.md's Illustrative section (line ~152-155) correctly says: "Since 2026-07-14 push-build.yml deploys production and production-eu in parallel" — matching push-build.yml's independent `update-stack` / `update-stack-eu` jobs (no `needs:` between them) and commit 9dece39785f5 ("Run production-eu in parallel...", 2026-07-14). But doc/runbooks/reference/pr-and-weekly-release-process.md:42 (mermaid: `it --> useu["update-stack-eu\n(production-eu)"]`) and :69 ("That will first update the [...]
    suggestion: The README is right to follow the workflow YAML (the executable source of truth) rather than the stale runbook prose, so no fixture change is needed. But since the README's stated audience is "the people who run that pipeline, so they can check it against what they know" — and what they know is this runbook — consider adding a one-line note [...]
    verify: The finding's factual claims reproduce on every point and none of the three refutation routes applies: (1) receipts exist for the runbook's sequential wording, push-build.yml's independent EU job, the 2026-07-14 parallelism commit, and the runbook's later edit date; (2) README's "Boundaries, on purpose" (README.md:204-214) covers [...]

=== REFUTED ===

=== LOW ===
- [sql-correctness] v_trace_cell's LIMIT 1 tie-breaks by ts alone, contradicting the file's own stated seq tie-break invariant @ views.sql:539 :: Add a deterministic secondary key, e.g. ORDER BY t2.reached_at, t2.freight (or thread through fr.seq), matching the seq-tiebreak convention used everywhere else in the file.
- [ledger-purity] SNAPSHOTS captions hardcode freight ids and stage names as page-visible prose, outside both SCREENS and self_checks @ render.py:50 :: If kept as hand-authored chrome, note the exception explicitly in the SCREENS-purity comment at render.py:517-518 so a future reader doesn't read the caption text as query-driven; or generate each caption fragment from the same predicate the [...]
- [scenario-fidelity] "staging integration tests as the branch-protection check" understates what the required check actually gates on @ /Users/claire/src/nyobe/delivery-ledger/README.md:128-129:None :: Reword to something like "the combined staging-deploy-and-integration-tests check as the branch-protection gate" to reflect that `staging-checks-pass` depends on both jobs, not integration tests alone.

=== COVERED WELL ===
- [sql-correctness]
    · Swept all 131 distinct fact timestamps rebuilding the db at each instant: v_grid always has exactly 5 rows once all 5 stages are declared (the only deviations are the 4 pre-declaration instants [...]
    · All 54 of the repo's own self_checks (render.py) pass unmodified, including the mutation checks for stale verification, wrong-role approval, rogue decisions, unevidenced decisions, duplicate [...]
    · v_grid's 'held' status arm (never exercised by the fixture's own timeline) was verified reachable and correct via a targeted mutation (production carries+verifies a new freight while production- [...]
    · The lens's suggested bug pattern ('a carried term for a freight production carried in the past') is already correctly handled and is pinned by an existing self-check: production-eu's gate for [...]
    · The scalar 2-arg max() used for gate_since (v_gate) and awaiting_since (v_gate_eval) is consistently wrapped in coalesce(...,'') on every argument, correctly avoiding SQLite's NULL-propagating [...]
    · v_pending_uptake's NULL-vs-boolean pending semantics verified correct against both AMI edges (US pending, EU not) and the image edge (auto-taken, pending=0).
    · v_release_prs's first-cut boundary (coalesce(max(...), '') then merged_at > '') correctly includes every PR in the very first release; PR lists for F416/F417/F418 verified against v_release_prs [...]
    · Latest-wins-by-seq tie handling for approvals and holds at identical timestamps verified via mutation (two approvals at the same ts still yield one v_gate_term row; a second concurrent hold [...]
- [ledger-purity]
    · fixture.py's rule_text() (fixture.py:68-83, used at fixture.py:134: `p = {"mode": mode, "terms": terms, "rule": rule_text(name, terms), ...}`) genuinely derives the policy's `rule` string from [...]
    · No forbidden derived-state keys or values anywhere in the emitted ledger: a direct scan of all 132 facts.jsonl payloads (recursive walk, same FORBIDDEN_KEYS={status,state} / [...]
    · The lint's payload walk (render.py:76-84, applied at render.py:120-124) recurses through nested dicts/lists, not just top-level payload keys, so it can't be defeated by nesting a forbidden [...]
    · v_grid's `status` column (views.sql:459-479) is a genuine SQL CASE under the view's own comment "No status is stored anywhere; every column is a join" — this is the correct version of the [...]
    · screen_grid, screen_lanes, screen_trace, and screen_diffgate all build their stage/column lists dynamically from a query (`SELECT stage FROM v_stage ORDER BY ord` or `SELECT DISTINCT stage FROM [...]
    · Section titles that make a live claim about state are genuinely row-derived at render time: screen_gates' heads (render.py ~607-624), screen_outofband's heads (render.py ~757-765), and [...]
    · No hardcoded transition ids appear anywhere in the SCREENS region of render.py (grepped lines 520-848 for `transition:` literals — zero hits); transition identity is always read off a row [...]
- [scenario-fidelity]
    · Release cron at 15:00 UTC on weekdays, with a second unmodelled 20:00 UTC Mon-Thu cut — matches release.yml:6,8 ('00 15 * * 1-5' and '00 20 * * 1-4') exactly.
    · Branch format `release/YYYY-MM-DD--HHMM` — matches release.yml:37,95 (TZ='America/Los_Angeles' date '+%Y-%m-%d--%H%M', BRANCH="release/${time-string}"); fixture.py:276,323,382 produce e.g. [...]
    · PR title format `release@<time-string>` and label `release` — matches release.yml:107,111 exactly; fixture.py:251 produces the same 'release@{branch-suffix}' with label 'release'.
    · Staging deploys from the release PR the moment it opens, with no precondition — matches pr-staging-deploy.yml:7-27 (fires on any pull_request targeting production, no gating `if:`); [...]
    · There is no staging-eu — matches pr-and-weekly-release-process.md:54 verbatim ('There is no `staging-eu`'); fixture.py's STAGES list (lines 99-111) omits it.
    · Production-eu parallelism date (2026-07-14) — verified via `git log` on push-build.yml: commit 9dece39785f5 titled 'Run production-eu in parallel; split PR staging deploy into its own workflow [...]
    · notify-deployed-prs job that can fail without failing the release — matches push-build.yml:63-65 (standalone job, needs:[update-stack] only, nothing downstream depends on it); fixture.py:347 [...]
    · Five ECS services (API, Console, Jobs, Ratelimit, Workflow) — matches rollback-the-pulumi-service.md:15-21; fixture.py's observed() dict (line 422) carries exactly {api, console, jobs, [...]
    · ~20-30 minute ECS task roll — matches emergency-deploy-the-pulumi-service.md:7 verbatim ('takes around 20-30 minutes to fully roll ECS tasks'); F417's modelled production transition runs [...]
    · PULUMI_DEPLOY_DEFAULT_IMAGE_REFERENCE image-reference binding, auto uptake, nothing to approve — matches rollback-the-pulumi-service.md:51 near-verbatim; fixture.py:155-157 quotes the same env [...]
    · Workflow AMI reaching the pool via launch template + instance refresh — matches rollback-the-pulumi-service.md:53-55.
    · Rollback as reverting ECS task definitions with a migration-safety check — matches cmd/prod-rollback/README.md:13-15 ('blocks rollback if migrations/ changed... requires explicit confirmation [...]
    · Testing deploy from the merge-queue SHA, currently disabled while the stack is restored — matches pr-build.yml:217-228 verbatim, including the 'DB user credentials are still out of sync with the [...]
    · testing-eu deploying from push-master.yml — matches push-master.yml:25-30 (update-eu job, target-stack: testing-eu).
    · HOTFIX/URGENT merge-queue break-glass correctly flagged as a different mechanism from the fixture's break-glass fact — matches pr-build.yml:184-207.
    · Merge as production's approval, via a merge commit (not squash) — matches pr-and-weekly-release-process.md:66.
    · AMI edge modelled as illustrative gated uptake vs. the real update-workflow-ami.yml auto-merged daily PR — matches update-workflow-ami.yml:4-6 (daily 15:10 UTC cron) and :97-101 (gh pr merge [...]
    · Content-keyed freight / lanes caption honestly labelled illustrative — render.py:586-591 states 'today pulumi-service rebuilds per stage from the git SHA, so this lane is the thesis's construct, [...]
    · Production-eu policy description ('Proposed: today EU deploys in parallel with US') verified present verbatim in facts.jsonl fact f011, and honestly frames the modelled sequential/gated edge as [...]
    · Wednesday testing-eu failure (ECS circuit breaker) and F419/F420 supersession correctly labelled 'plausible, not from the record' rather than grounded.
    · facts.jsonl is in sync with fixture.py (spot-checked the production-eu policy text and F418's PR number 14882 in both); `./render.py --lint-only` reports 132 facts, clean, 54/54 self-checks passing.
    · Run #418 / PR #14882 borrowed from an external fixture (Vic's SERVICE_RELEASE) is honestly flagged as borrowed rather than claimed as grounded in pulumi-service.

=== CLOSURE ===
Counter({'fixed': 20, 'recorded': 6, 'open': 3, 'partially': 2})
- [partially] 2. screen titles/blurbs were static prose asserting fixture state false in other snapshots :: Most screens fixed: render.py:517 comment 'no stage or freight is named in code', stages/heads built from queries. But screen_uptake's blurb (render.py:729-731) still says 'The AMI bake published a new record and the worker pool's binding is gated, so the grid shows available, gated' as fixed [...]
- [recorded] 7. v_lanes/v_trace super-linear (15s/47s at ~100 freights) :: smells.md #13 ('Cost'): 'The verification pass measured the lanes/trace views at ~15s and ~47s per query with 100 synthetic freights ... Not fixed: at fixture scale it is invisible ... Worth carrying into OPEN 5 as a data point: Supabase-scale is 21k updates per 30 days.'
- [recorded] 10. production-eu modelled downstream of production; actually parallel since 2026-07-14 :: README.md 'Illustrative' section: 'production-eu as a sequential, auto-if-safe follower of production. Since 2026-07-14 push-build.yml deploys production and production-eu in parallel. The fixture's edge is a proposed policy ...' Also carried into the fact itself: facts.jsonl:11 [...]
- [recorded] 13. 'testing on every master push' stale; 'break-glass' collides with the HOTFIX [...] :: README.md 'Illustrative': 'Today testing deploys pre-merge from the merge-queue SHA (pr-build.yml, currently disabled while the stack is restored)' — matches real repo pulumi-service/.github/workflows/pr-build.yml:219 'Temporarily disabled while the `testing` stack is being restored from [...]
- [recorded] 14. freight identity: each stage rebuilds from git SHA, prod at merge commit; page [...] :: README.md 'Illustrative': 'content-keyed freight moving unchanged through the stages. Today each stage rebuilds from the git SHA and production builds the merge commit, not master's head; the lanes caption says so.' render.py:634-635 (screen_lanes blurb) now states: 'today pulumi-service [...]
- [recorded] 15. AMI mechanism is update-workflow-ami.yml (auto-merged PR riding the train), [...] :: README.md 'Illustrative': 'The real mechanism is update-workflow-ami.yml: a daily auto-merged PR to master that rides the release train.' Confirmed against real repo: pulumi-service/.github/workflows/update-workflow-ami.yml:6 `cron: "10 15 * * *"` (daily) and line 99 `gh pr merge ... --squash [...]
- [recorded] 16. job.finished written by facts, read by no view; 'optional' is intent in an [...] :: smells.md #10: '"optional" on job.finished is intent living in an observation. ... It should be declared (a side-job declaration on the stage) and the observation should carry only the outcome. Left in the payload for now; the transition detail renders it.' program.md:106 repeats the same [...]
- [open] 28. (low) pending-uptake preview is a synthesized string, not a plan fact :: views.sql:600-602 v_pending_uptake.preview is still built by SQL string concatenation: `'preview ' || e.consumer || ' with ' || e.key || ' = ' || json_extract(r.payload,'$.values.'||e.key)`. No entry in smells.md or README acknowledges this (`grep -n preview smells.md README.md` shows no [...]
- [partially] 29. (low) program declarations that write nothing (warehouse/verify/enact); [...] :: The 'warehouse:master undeclared' half is fixed: facts.jsonl:1 now has a real `warehouse.declared` fact for `warehouse:master`, and lint (render.py:132-135) checks stage upstreams reference a declared warehouse. The 'writes nothing' half is honestly recorded, but in program.md rather than [...]
- [open] 30. architecture text lists 'freight declarations' under intent while [...] :: Out of this repo's scope (thesis-architecture.md lives in the notebook, not delivery-ledger) but still wrong: /Users/claire/src/nyobe/claudes-notebook/projects/delivery-as-code/thesis-architecture.md:86 still lists 'freight declarations' under 'Intent facts'. The delivery-ledger repo itself [...]
- [open] extra (not in original 30): README claims '51 self-checks' but the current run [...] :: README.md 'Discipline the fixture is held to' section: '51 self-checks pass'. Actual: `python3 render.py` → 'self-check: 54/54 passed'. Minor doc drift introduced by this revision's added checks (new mutation/time-travel cases) without updating the stated count.
fixed: ['1. diff-gate screen re-implemented the gate in Python [...]', '3. screens hardcoded stage names and transition ids', '4. v_gate_eval.passes NULL when production-eu had no [...]', '5. one-row-per-stage broke on a second active hold / [...]', "6. approval term dropped role='oncall'", "8. v_grid '<>' comparisons NULL when no decision → [...]", '9. awaiting_since omitted hold/plan/approval instants', '11. staging gated on testing verification; real [...]', '12. release cut time (15:00 UTC) and branch naming [...]', '17. f015 policy decision cited evidence not in the [...]', '18. failure path never exercised (no failed transition)', '19. OPEN 1 not exercisable on the edge axis (both [...]', '20. supersession had no fixture instance; in-flight [...]', '21. gate terms not data; rule text hand-compiled twice', '22. (escaped mutation) a verification recorded before [...]', '23. (low) out-of-band screen re-evaluated per-service [...]', '24. (low) v_plan.safe NULL when migrations_changed [...]', '25. (low) v_pending_uptake.pending NULL before any [...]', "26. (low) README called click-ops force-deploy 'the [...]", '27. (low) Keith parity table omitted in-flight [...]']
