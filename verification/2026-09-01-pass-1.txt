top keys: ['summary', 'agentCount', 'logs', 'result', 'workflowProgress', 'totalTokens', 'totalToolCalls']
result keys: ['mutations', 'confirmed', 'refuted', 'low', 'covered_well']

=== MUTATIONS ===
- [CAUGHT] lint=True self=False :: Delete fact f056 (approval.granted, oncall approval for F417 on stage:production); leave f057 (promotion.decided, [...]
    fails: ['lint: f057 (line 56): ref f056 does not point at an earlier fact']
    note: Copy at /private/tmp/claude-501/-Users-claire-src-nyobe-claudes-notebook/921145c0-40ea-4b04-a4fc-feebddc4fdcc/scratchpad/mut-no-approval.jsonl; diff vs original is exactly the one deleted line (108 -> 107 lines). Command output, exit 1: lint: 1 problem(s) f057 (line 56): ref f056 does not point at an earlier fact Caught by lint's ref-integrity check (dangling ref), not by any self-check — self-checks did not run because lint failed first. What this shows: the mechanism holds here only because f057 carries a ref to its gate. The ledger has no semantic 'promotion.decided must be preceded by approval.granted for the same freight/stage' [...]
- [CAUGHT] lint=False self=True :: f093 (staging integration-tests for F418, actor ci:gha, run gha:pr-build/7841) payload.outcome 'pass' -> 'fail'. [...]
    fails: ['grid: production is awaiting oncall approval, not failed', 'trace: #46173 reached staging, next is production approval']
    note: Command output (exit=1): self-check: 2/24 failed ✗ grid: production is awaiting oncall approval, not failed ✗ trace: #46173 reached staging, next is production approval lint: 107 facts, clean Lint is clean, and should be: a verification.recorded fact with outcome 'fail' is a perfectly well-formed fact. Lint checks shape/ordering/referential integrity, not whether the recorded world is the world the author expected. The self-checks are the layer that caught it, because they pin the fixture's expected derived state at DEFAULT_AS_OF. What the views actually say under the mutation (queried via render.build_db/q): v_grid production: [...]
- [CAUGHT] lint=False self=True :: Delete fact f073 (breakglass.recorded, INC-2311, api rolled back F417→F416 on production) from a copy of [...]
    fails: ['grid: production drift is explained by the break-glass fact']
    note: Command output (exit 1): self-check: 1/24 failed ✗ grid: production drift is explained by the break-glass fact lint: 106 facts, clean Lint is structural (schema/ordering/refs) and correctly stays clean: deleting a whole fact leaves a well-formed, append-only ledger (nothing referenced f073, so no dangling ref). The self-check at render.py:215-216 asserts "INC-2311" in v_grid.drift for production; querying the mutated db gives v_grid production = {status: 'awaiting', carried: 'F417', drift: 'api@F416 — UNEXPLAINED'} and v_breakglass = []. So the views behave exactly as designed — observed state (api@F416) diverges from desired (F417) [...]
- [CAUGHT] lint=False self=True :: f083 payload.version 13 -> 12 (AMI publication no longer newer than the uptaken v12); copy at [...]
    fails: ['uptake: production edge pending (v13 > v12); production-eu edge not pending (v9 == v9)']
    note: Command output (exit=1): self-check: 1/24 failed ✗ uptake: production edge pending (v13 > v12); production-eu edge not pending (v9 == v9) lint: 107 facts, clean Lint is clean, as it should be: the mutated file is structurally valid and lint has no business knowing what version the bake "ought" to have published. The self-check at render.py:238-240 caught it because it asserts the fixture's story (v_pending_uptake.pending == 1 for workflow-pool@production); with v13 -> v12 the view now reports published_version=12, consumed_version=12, pending=0 (queried via build_db: production row pending=0, production-eu row pending=0). So the [...]
- [CAUGHT] lint=False self=True :: f084 payload.until 2026-08-26T14:00:00Z -> 2026-08-26T11:00:00Z (hold on stage:production-eu expires before [...]
    fails: ['grid: production-eu is converged (candidate == desired) and held']
    note: Output (exit 1): "self-check: 1/24 failed / ✗ grid: production-eu is converged (candidate == desired) and held / lint: 107 facts, clean". Lint is clean, as expected — a hold that expires at 11:00 is a perfectly well-formed fact; lint checks shape/ordering, not fixture semantics. The self-check caught it because the check (render.py:217-219) asserts hold_until IS NOT NULL for production-eu at the primary as-of, and the mutated v_grid row now shows held_until=None, hold_until=None, hold_by=None, hold_rationale=None, status='converged', drift=None. So the views behave correctly: an expired hold is temporally scoped out at as-of 11:45, and [...]
- [ESCAPED] lint=False self=False :: verify-before-deploy: fact f093 (staging integration-tests pass for F418) moved from ts 2026-08-26T09:29:00Z to [...]
    fails: []
    note: Command: `python3 /Users/claire/src/nyobe/delivery-ledger/render.py --lint-only --facts /private/tmp/claude-501/-Users-claire-src-nyobe-claudes-notebook/921145c0-40ea-4b04-a4fc-feebddc4fdcc/scratchpad/mut-verify-before-deploy.jsonl` -> stdout `lint: 107 facts, clean` / `self-check: 24/24 passed`, no stderr, exit 0. Why it escaped. lint() (render.py:82-155) checks only syntactic/ordering invariants: monotone ts, declared stages/freight, transition start/finish pairing, forbidden derived-state keys. It has no rule relating verification.recorded to v_carried. The self-checks (render.py:201-283) all pin end-state answers as of [...]
- [CAUGHT] lint=False self=True :: Append f108 @ 2026-08-26T11:40:00Z: class intent, kind promotion.decided, subject stage:production, actor [...]
    fails: ['grid: production is awaiting oncall approval, not failed', 'lanes: F418 has not reached production and is the awaiting freight there', "trace: #46173 reached staging, next is production approval — raised AttributeError: 'NoneType' object has no attribute 'startswith'", 'observed: production has exactly one mismatched service (api@F416)']
    note: Command: `python3 render.py --lint-only --facts <copy>` -> exit 1. Output verbatim: ``` self-check: 4/24 failed ✗ grid: production is awaiting oncall approval, not failed ✗ lanes: F418 has not reached production and is the awaiting freight there ✗ trace: #46173 reached staging, next is production approval — raised AttributeError: 'NoneType' object has no attribute 'startswith' ✗ observed: production has exactly one mismatched service (api@F416) lint: 108 facts, clean ``` Lint is structural only (108 facts, clean): it does not know that a promotion.decided on production requires a preceding approval.granted, so the schema layer accepts [...]
- [CAUGHT] lint=False self=True :: Delete facts f074 and f106 (the two state.observed conformance reads of stage:production by watch:ecs). 107 -> 105 [...]
    fails: ['grid: production drift is explained by the break-glass fact', "observed: production has exactly one mismatched service (api@F416) — raised TypeError: 'NoneType' object is not subscriptable"]
    note: Output of `render.py --lint-only --facts <copy>` (exit 1): self-check: 2/24 failed ✗ grid: production drift is explained by the break-glass fact ✗ observed: production has exactly one mismatched service (api@F416) — raised TypeError: 'NoneType' object is not subscriptable lint: 105 facts, clean Lint is clean (as it should be — the ledger is structurally valid; there is nothing lint-shaped about a missing observation). The self-check caught it only because the fixture-specific expectations pin production to "drifted, explained by INC-2311"; the second check failed by crashing (one() returned None for v_observed WHERE stage='production'), [...]

=== CONFIRMED FINDINGS ===
- (high) [ledger-purity] Diff-gate screen re-implements the production-eu gate in Python and disagrees with v_gate_eval at the same instant  @ /Users/claire/src/nyobe/delivery-ledger/render.py:527
    evidence: render.py:527-533 decides the 'gate outcome' column from two columns only: if r["plan_safe"]: outcome = '...auto</span><div class="small">safe plan — no approval needed</div>' elif r["approved_at"]: outcome = '...approved...' else: outcome = '...would require approval...' views.sql:304-307 (the SQL gate for the same stage) also requires g.upstream_carried_at IS NOT NULL, g.upstream_integration_at IS NOT NULL and g.held_until IS NULL. [...]
    suggestion: Hoist the three-way outcome into SQL as a column over all (stage, freight) pairs — e.g. extend v_gate_all (or a v_gate_outcome view) with the same CASE v_gate_eval uses, including held_until and upstream_carried_at — and have screen_diffgate render that column. v_gate_eval cannot serve as-is because its JOIN to v_candidate keeps only the [...]
    verify: Not refuted: this is a real gap in this slice, not a declared boundary. None of the README "Boundaries, on purpose" items (no pump/executors/engine changes, no ingest, no real output-record publication, tables-not-chrome) cover it, and it is absent from smells.md — the project's second [...]
- (medium) [ledger-purity] Screen titles/blurbs are static prose asserting fixture state that is false in two of the three shipped snapshots  @ /Users/claire/src/nyobe/delivery-ledger/render.py:477
    evidence: render.py:477-480 (screen_awaiting) hardcodes the title "Production is waiting, not broken" and blurb "What it lacks is an approval fact; until one arrives the stage is <em>awaiting</em>". Rendered text at snapshot 2026-08-24T10:20:00Z and 2026-08-24T10:50:00Z: "Production is waiting, not broken ... What it lacks is an approval fact; until one arrives the stage is awaiting ... Candidate F417 for production (policy: gated). Gate ... passes." [...]
    suggestion: Either derive the sentence from the row (verdict-driven title: 'waiting' vs 'passes' vs 'in flight' chosen from v_gate_eval/v_grid columns, freight names interpolated from the query) or move the narrative into the snapshot captions / a fixture-specific commentary block that is rendered only for the primary snapshot. The blurbs should not make [...]
    verify: The headline claim reproduces exactly as stated and is visible in the shipped out/index.html, not just a re-render: screen_awaiting (render.py:477-480) emits the static title "Production is waiting, not broken" and blurb "What it lacks is an approval fact; until one arrives the stage is [...]
- (medium) [ledger-purity] Screens hardcode stage names and transition ids, making four of ten screens fixture-specific rather than query-driven  @ /Users/claire/src/nyobe/delivery-ledger/render.py:617
    evidence: screen_transitions, render.py:617: q(db, "SELECT * FROM v_transition WHERE transition IN ('transition:T417-prod','transition:T418-staging') ORDER BY started_at") screen_awaiting, render.py:450-451: "SELECT * FROM v_gate_eval WHERE stage='production'", "SELECT * FROM v_policy WHERE stage='production'"; and the evidence lookups at :457, :459, :461 all use "... FROM v_verified WHERE stage='staging' AND freight=? AND chk='...'" while the SQL [...]
    suggestion: Drive each screen from the query: awaiting = the stage(s) whose v_grid.status = 'awaiting' (or every gated stage), with requirement rows generated from v_gate_all's non-null/null term columns and evidence looked up via v_stage.verify_in; diffgate = every stage whose policy mode is 'auto-if-safe'; out-of-band = every stage with a v_observed [...]
    verify: Not refuted. The hardcoding is confirmed by the quoted lines, it is not covered by README "Boundaries, on purpose" (pump/executors/engine, ingest, output-record publication, chrome), and smells.md -- the repo's own acceptance log for this class of thing -- admits only the SQL hand-compile [...]
- (high) [sql-correctness] v_gate_eval.passes is NULL (not 0) when production-eu has no plan yet; v_grid then reports 'converged' while a candidate is awaiting a plan  @ /Users/claire/src/nyobe/delivery-ledger/views.sql:307
    evidence: views.sql:307 `AND (g.plan_safe = 1 OR g.approved_at IS NOT NULL))` — with plan_safe NULL and no approval this is NULL OR 0 = NULL, and views.sql:358-361 test `ge.passes = 0` / `ge.passes = 1`, both false on NULL, so v_grid falls through to `d.freight = k.freight` → 'converged'. as-of 2026-08-24T10:44:30Z (between production's integration pass f066 @10:44 and the EU plan f067 @10:45): gate {"stage": "production-eu", "freight": "F417", [...]
    suggestion: Make every boolean column NULL-free: `coalesce(g.plan_safe, 0) = 1` at :307 (or write `passes` as CASE WHEN … THEN 1 ELSE 0 END), and in v_grid compare with `coalesce(ge.passes, 0)` / `IS NOT`. Add a self-check at 2026-08-24T10:44:30Z pinning production-eu = 'awaiting' + 'plan for production-eu' so the plan-missing branch has a negative case.
    verify: Confirmed as a real defect in this slice, not a declared boundary. The decisive receipt is that views.sql itself models the plan-missing state: line 321 writes `WHEN g.plan_safe IS NULL THEN 'plan for production-eu'` into `awaiting`, but the `passes` column on line 307 leaks NULL for the same [...]
- (high) [sql-correctness] v_grid's one-row-per-stage invariant breaks on inputs lint allows: a second active hold, a re-cut freight, or an identical-ts decision duplicates rows; the scalar subqueries silently keep the first hit  @ /Users/claire/src/nyobe/delivery-ledger/views.sql:381
    evidence: (a) second hold.placed on production-eu (until 18:00, lint clean): v_grid row count: 6; production-eu rows: held_until 14:00 / hold_by user:priya AND held_until 14:00 / hold_by user:sam. views.sql:381 `LEFT JOIN v_hold h ON h.stage = s.stage` fans out; views.sql:285 `(SELECT h.until_ts FROM v_hold h WHERE h.stage = s.stage)` returns 14:00 although the stage stays held until 18:00. (b) two approvals for production/F418 (maya 11:40, sam [...]
    suggestion: Dedup by seq, not ts: latest-wins via `f.seq = (SELECT max(g.seq) …)` (or `row_number() OVER (PARTITION BY subject ORDER BY ts DESC, seq DESC) = 1`) in every per-subject view, including v_freight's release.cut join. For holds, aggregate rather than pick: `max(until_ts)` per stage in v_gate_all, and join v_grid to a one-row-per-stage hold [...]
    verify: Not refuted: the finding is a real gap in this slice as scoped, not a restatement of a declared boundary. README "Boundaries, on purpose" excludes pump/executors/engine changes, ingest, real output-record publication, and chrome; nothing there (nor in smells.md #3/#7, which are adjacent but [...]
- (medium) [sql-correctness] The compiled approval term drops the declared `role = 'oncall'` predicate — any approval.granted role opens production  @ /Users/claire/src/nyobe/delivery-ledger/views.sql:273
    evidence: Policy f009 rule: `… AND approved(production, F, role = 'oncall')`; v_approval exposes `role` (views.sql:161) but views.sql:273-278 select `WHERE a.stage = s.stage AND a.freight = fr.freight` only. Injected approval.granted production/F418 with {"role": "qa"} by user:dev1 (lint errors: []): gate_eval: [{'stage': 'production', 'freight': 'F418', 'passes': 1, 'awaiting': None, 'approved_by': 'user:dev1'}] grid : [{'stage': 'production', [...]
    suggestion: Add `AND a.role = 'oncall'` to the three approval subqueries (or carry the role through v_gate_all as `approved_role` and test it in v_gate_eval), and add a self-check that a non-oncall approval does not flip production to 'ready'. This is a divergence between rule text and its hand compilation, distinct from smells.md #2.
    verify: Reproduced with a concrete receipt, and no declared boundary covers it. The policy fact f009 declares production's rule as `… AND approved(production, F, role = 'oncall')`, and v_approval exposes `role` (views.sql:161), but v_gate_all's three approval subqueries (views.sql:273-278) filter only [...]
- (medium) [sql-correctness] v_lanes / v_trace / v_trace_summary are super-linear in freight count: 15 s / 47 s per query at ~100 freights; materializing the per-subject layer once makes them ~ms  @ /Users/claire/src/nyobe/delivery-ledger/views.sql:385
    evidence: Timing SELECT * FROM <view> (ms), synthetic master freights appended (discovery+testing promotion+transition+verification each), sqlite 3.51.0: baseline 107 facts: v_lanes 3.5 v_trace 4.9 v_trace_summary 15.0 +25 freights (232 f): v_lanes 136 v_trace 181 v_trace_summary 480 +50 freights (357 f): v_lanes 1309 v_trace 1336 v_trace_summary 3929 +100 freights (607 f): v_lanes 15005 v_trace 15814 v_trace_summary 47236 (render_page runs every [...]
    suggestion: Keep the views as the definition but let build_db materialize the per-subject layer (`CREATE TABLE … AS SELECT * FROM v_transition` etc.) before the derived views run — it is still derived from facts per render, so it does not violate the no-stored-state rule. Add an index on (kind, subject, ts) so the latest-per-subject lookups stop scanning [...]
    verify: Not refuted. The finding reproduces with receipts, and no declared boundary covers it. (1) Reproduction: using sys.path.insert(0,'/Users/claire/src/nyobe/delivery-ledger'); import render; render.build_db(facts + synthetic, render.DEFAULT_AS_OF) with 5 facts per synthetic master freight [...]
- (medium) [sql-correctness] v_grid status comparisons with `<>` go NULL when no promotion has been decided (or nothing carried yet): 'pending' masks 'ready', 'awaiting' and 'failed'  @ /Users/claire/src/nyobe/delivery-ledger/views.sql:355
    evidence: views.sql:355 `lf.outcome = 'failed' AND d.freight <> k.freight`, :356/:358/:360 `ge.freight <> d.freight` — NULL when d.freight or k.freight is NULL, so every branch is skipped and :363 yields 'pending'. v_lanes:394 already handles this correctly with `(d.freight IS NULL OR d.freight <> fr.freight)`. as-of 2026-08-18T09:00:30Z (30 s after the F416 cut, before f015): staging {desired: null, carried: null, candidate: F416, awaiting: [...]
    suggestion: Use SQLite's NULL-safe `IS NOT` / `IS` for the freight comparisons in v_grid's status CASE (as v_lanes already does), and add a self-check at 2026-08-18T09:00:30Z (staging 'awaiting', no decision yet) to pin the pre-decision path.
    verify: Confirmed, not a declared boundary. README "Boundaries, on purpose" lists no pump / no executors / no engine changes / no ingest / output-record publication blocked / tables-not-chrome; smells.md has no entry either. NULL-handling in v_grid's status CASE is inside the slice's own contract [...]
- (medium) [sql-correctness] awaiting_since omits the hold, plan and approval instants, so 'since' can predate the condition it describes — by a minute in the shipped Mon 10:50 snapshot, by two days for the hold  @ /Users/claire/src/nyobe/delivery-ledger/views.sql:325
    evidence: views.sql:325-329 `max(coalesce(c.available_at,''), upstream_integration_at, staging_smoke_at, staging_loadgen_at, upstream_carried_at)` — no plan ts (v_gate_all passes plan_fact but not v_plan.ts), no v_hold.placed_at, no approval ts. Default as-of, v_gate_eval: {"stage": "production-eu", "awaiting": "hold until 2026-08-26T14:00:00Z", "awaiting_since": "2026-08-24T10:44:00Z"} while the hold f084 was placed 2026-08-26T08:50:00Z (masked in [...]
    suggestion: Pass v_plan.ts and v_hold.placed_at through v_gate_all and include them (coalesced) in the max, or better derive awaiting_since per awaiting-term: the time of the newest fact the gate read to reach that verdict (for 'hold until …' → placed_at; for 'approval…(plan not safe)' → plan ts).
    verify: Confirmed as a real defect in this slice, not a declared boundary. README "Boundaries, on purpose" excludes pump/executors/engine changes, ingest, real output-record publication, and chrome — nothing about gate timing. smells.md #8 is not an exclusion either; it asserts correctness ("it is [...]
- (high) [scenario-fidelity] production-eu is modelled as downstream of production's integration tests; since 2026-07-14 it deploys in parallel with production  @ /Users/claire/src/nyobe/delivery-ledger/README.md:88
    evidence: README.md:86-89 (under 'Grounded in the runbook'): "production, then production-eu after production's integration tests"; README.md:110: "Today EU follows US unconditionally". facts.jsonl:5 (f005): "upstream":"production","verify_in":"production"; facts.jsonl:10 (f010) rule: "carried(production, F) AND verified(production, F, 'integration-tests') ..."; program.md:47-49 same. Pipeline: .github/workflows/push-build.yml:47-53 — update-stack- [...]
    suggestion: Answer to the task's question: production-eu waits on neither production's integration tests nor the sentry job — it starts with the push to `production`, concurrently with the US update. This is the one fact that would most improve recognisability: make production-eu a sibling of production (f005 upstream/verify_in = staging; f010's [...]
    verify: Confirmed as a real scenario-fidelity defect in this slice, not a restatement of a declared boundary. The sequential EU edge is asserted in the README's "Grounded in the runbook" (real) list at README.md:86-89 ("production, then production-eu after production's integration tests"), not in [...]
- (medium) [scenario-fidelity] Staging is gated in the fixture on testing's integration tests; the real trigger is PR-open against production, unconditionally  @ /Users/claire/src/nyobe/delivery-ledger/facts.jsonl:8
    evidence: facts.jsonl:8 (f008): "mode":"auto","trigger":"release.cut","rule":"verified(testing, F, 'integration-tests')"; program.md:37: `promote: d.auto({ requires: [d.verified("testing", "integration-tests")] })`; f015/f049/f086 rationales cite 'verified in testing'. Pipeline: .github/workflows/pr-staging-deploy.yml:7-10 — on: pull_request: branches: - production — deploy-staging (lines 21-27) has no `needs`; release.yml's only preconditions before [...]
    suggestion: Make f008's rule `true` with trigger `release.cut` (the PR opening is the trigger and there is no verification precondition), or label the testing-verified precondition as a proposed policy. Point staging transitions at `gha:pr-staging-deploy/...` so they match what Keith's tracker and the Actions tab call it.
    verify: The finding's core holds and is not a declared boundary. (1) The real pipeline deploys staging unconditionally on PR-open against production: pr-staging-deploy.yml:7-10 triggers on `pull_request: branches: [production]`, `deploy-staging` (lines 21-27) has no `needs`, and release.yml (read to [...]
- (medium) [scenario-fidelity] Release cut time and branch naming don't match release.yml: cron is 15:00 UTC Mon-Fri + 20:00 UTC Mon-Thu, branches are release/YYYY-MM-DD--HHMM, and 'release/staging@' never existed in the workflow  @ /Users/claire/src/nyobe/delivery-ledger/facts.jsonl:14
    evidence: facts.jsonl:14 (f014): "ts":"2026-08-18T09:00:00Z", "actor":"cron:release-09utc", "branch":"release/staging@08182026-0900", "armed_by":"user:priya" (same shape at :48, :85). README.md:90 lists as real: "the release cron; `release/staging@MMDDYYYY-HHMM` branch names". Pipeline: .github/workflows/release.yml:6-8 — # set release to 7/8 am PST/PDT on weekdays. - cron: '00 15 * * 1-5' # add a second release at 12/1 pm PST/PDT on Mon - Thurs. - [...]
    suggestion: Cut at 15:00Z (and, if the second daily cut matters to the story, 20:00Z Mon-Thu) with actor `cron:release-15utc`; name branches `release/2026-08-18--0800` (pre-08-24) / `release/2026-08-26--0800`, PR title `release@...`, label `release`; drop `armed_by` from scheduled cuts. Either fix README.md:90 to the workflow's format or label the branch [...]
    verify: The core of the finding reproduces and is not a declared boundary: README.md:90 lists "the release cron; `release/staging@MMDDYYYY-HHMM` branch names" under "What is real", but the pulumi-service workflow has cut at 15:00 UTC (never 09:00 UTC) since 2021 and has not produced [...]
- (medium) [scenario-fidelity] 'testing on every master push' is stale: testing deploys pre-merge from the merge-queue SHA (currently disabled), and the pipeline already has a 'break-glass' that means something else  @ /Users/claire/src/nyobe/delivery-ledger/README.md:86
    evidence: README.md:86: "testing/testing-eu on every master push"; facts.jsonl:6 (f006): "trigger":"freight.discovered where source.branch = 'master'" ... "Every master build rolls to testing". Pipeline: .github/workflows/push-master.yml:17-23 — # With merge queue enabled, PRs are rebased and tested against latest # master in `merge_group` ... The `testing` deploy also happens # pre-merge in pr-build.yml's `deploy-testing`, so this workflow only: # - [...]
    suggestion: Say 'testing-eu on every master push; testing from the merge queue before the SHA lands (today disabled while the stack is restored)'. Consider renaming the side-door kind (e.g. `override.recorded`) or, better, model the real break-glass too: a HOTFIX merge-queue bypass is an intent with a rationale that today is a substring grep — a very [...]
    verify: Confirmed as a real (small) scenario-fidelity defect in this slice as scoped, not a restated boundary. README.md:86 places "testing/testing-eu on every master push" under "What is real" (intro line 5-7 says the point is that "the people who run that pipeline can check it against what they [...]
- (medium) [scenario-fidelity] Freight identity: every stage rebuilds its own images from the git SHA, and production's SHA is the merge commit, not master's — the fixture and rendered page say the testing build is the release  @ /Users/claire/src/nyobe/delivery-ledger/render.py:445
    evidence: render.py:444-445 (rendered lanes caption): "The same content-keyed freight moves through every stage — the testing build <em>is</em> the release." facts.jsonl:40 (f040) keys F417 on "sha":"7c9e2b4","branch":"master" with image digests, reused unchanged through staging/production/production-eu; facts.jsonl:74 (f074) maps "task-definition SHA tags" straight to F416/F417. Pipeline: .github/workflows/update-stack.yml:198 `run: make [...]
    suggestion: Label the content-keyed freight as the thesis's construct rather than today's artefact (today: rebuild per stage, keyed by git SHA, prod at the merge-commit SHA), and carry head SHA + merge-commit SHA on the release like Keith does so state.observed's ECS tags have something real to join on.
    verify: Confirmed as a real scenario-fidelity gap in this slice, not a restatement of a declared boundary. The rendered lanes caption asserts, in present tense and three times on the page, that "the testing build is the release" and that "the same content-keyed freight moves through every stage". [...]
- (medium) [scenario-fidelity] The workflow-AMI uptake mechanism is in the repo and differs from the fixture: daily auto-merged PR to master that rides the release train, not a gated manual uptake on a separate workflow-pool stack  @ /Users/claire/src/nyobe/delivery-ledger/README.md:106
    evidence: README.md:106-108: "the forward direction of the AMI edge as a *gated uptake with a versioned record* — inferred from how rollback walks it; pulumi-service-ami-rotate wasn't read for this". facts.jsonl:11 (f011): "uptake":"gated" ... "Approve the trigger; never auto-uptake"; :34-39 (f034-f039) `uptake.decided` by a person followed by `gha:update-stack/7751` against stack `workflow-pool`. Pipeline (in pulumi-service, not a separate repo): [...]
    suggestion: Labelled illustrative, so not a mislabel — but the disclaimer names the wrong source, and the real mechanism is a stronger demonstration of the thesis's 'uptake is a promotion that rides freight' than the invented gated edge. Either re-cite (update-workflow-ami.yml) and say the fixture deliberately inverts today's auto-uptake to show the [...]
    verify: Substantially confirmed, low severity, with one correction to the finding. Confirmed: (1) README.md:106-108's provenance line names the wrong source. `pulumi-service-ami-rotate` is, per its own GitHub description, "Rotates the Pulumi service ECS cluster onto a new AMI" — the service-fleet ASG [...]
- (high) [coverage-and-thesis] `job.finished` is written by three facts and read by no view; `optional` is program intent living inside an observation payload  @ /Users/claire/src/nyobe/delivery-ledger/facts.jsonl:65
    evidence: `grep -n "job.finished\|optional" views.sql render.py` → only `render.py:31` (the OBSERVATION_KINDS whitelist); no view, no screen, no self-check reads it. f065: `{"freight":"F417","job":"notify-deployed-prs","outcome":"failed","optional":true,"detail":"GitHub rate-limited; nothing downstream reads this"}`; f064/f092 `sentry-release` with `optional:false`. README:91 grounds the fixture in "the 'notify deployed PRs' job that may fail without [...]
    suggestion: Render side jobs (on the transition detail or as a grid note) and add a self-check pair: a failed `optional:true` job leaves the stage status unchanged; a failed `optional:false` job surfaces (as a gate term or a `failed` cause). Then move `optional` out of the observation: the program declares side jobs and their optionality (a [...]
    verify: Not refuted. The finding is a real, low-severity coverage gap in the slice's own terms, not a restatement of a declared boundary. README:143-150 ("Boundaries, on purpose") excludes pump/executors/engine, ingest, real output-record publication, and chrome — nothing about side jobs or [...]
- (medium) [coverage-and-thesis] f015 (policy:staging) records a decision whose cited evidence is not in the ledger; nothing checks that a policy's refs satisfy its rule  @ /Users/claire/src/nyobe/delivery-ledger/facts.jsonl:15
    evidence: f015 rationale `"release PR #14870 opened; F416 verified in testing"`, refs `["f014"]` (the cut only). No `transition:T416-testing*` and no `verification.recorded stage:testing` for F416 exists anywhere in the fixture. Building the ledger at f015's own ts: `v_gate_eval` staging×F416 → `{'passes': 0, 'awaiting': 'verification: integration-tests in testing', 'upstream_integration_at': None}`. program.md:89: "actor `policy:<s>`, `refs` = the [...]
    suggestion: Seed F416's testing leg (two `promotion.decided`, two transitions, one `verification.recorded`, all before 2026-08-18T09:00) and add a self-check: for every `promotion.decided` with actor `policy:*`, `v_gate_eval` at that fact's ts passes for (stage, freight) and the refs include the satisfying facts. This is the one place the fixture breaks [...]
    verify: Not refuted. The finding reproduces exactly and is not covered by any declared boundary. f015 (policy:staging, freight F416) is the only one of the fixture's 13 `policy:*` promotion.decided facts whose gate does not pass at its own timestamp; the other 12 pass. Its rationale asserts "F416 [...]
- (medium) [coverage-and-thesis] The failure path is never exercised: no failed transition, no self-check on `failed`, Keith's deploy-failure need (#3) absent; README's counts and claims outrun the fixture  @ /Users/claire/src/nyobe/delivery-ledger/README.md:130
    evidence: README:130 "a failed `transition.finished` renders as `failed`; step-level deep links are payload". `grep -c '"outcome": "failed"' facts.jsonl` → 0 among transitions (the only failed outcome is the unread job f065). No self-check names status `failed`; the arm at views.sql:355 `WHEN lf.outcome = 'failed' AND d.freight <> k.freight THEN 'failed'` has never run against data. keith_rollout_dashboard.md:114-117 (pain #3: failed step name, deep [...]
    suggestion: Two facts: a `transition.finished` with `outcome: failed` and a `resource.step` carrying the failing URN/error for one leg (e.g., a testing-eu run for F419, later retried), plus a self-check that the grid renders `failed` with the step and that a retry returns it to in-flight. Fix the count at README:72.
    verify: Confirmed as an in-scope coverage-and-documentation gap, not a restated boundary. Two corrections to the finding's wording: (1) "No self-check names status `failed`" is literally false — render.py:209 and :223 both name it — but both are assert-absence checks, and a mutation test proves they [...]
- (medium) [coverage-and-thesis] OPEN 1 (default uptake policy) is not exercisable: both declared edges are gated, so the sketch's "does auto-propagation feel safe?" criterion has nothing to show on the edge axis  @ /Users/claire/src/nyobe/delivery-ledger/facts.jsonl:11
    evidence: f011 `"uptake":"gated"` and f012 `"uptake":"gated"` are the only `binding.declared` facts. poc-ledger-tracker.md:83-87: "watching auto-propagation fill the grid, does it feel safe or surprising? Where did you *want* a gate? ... The POC exists to confirm or break this." thesis-architecture.md:259-269 marks the default OPEN and per-edge. The stage axis does exercise auto (f006/f007 testing, f010 auto-if-safe); the edge axis — where OPEN 1 [...]
    suggestion: One `binding.declared` with `uptake: "auto"` on a low-blast edge (a record the service stack consumes within the same stage), one `output.published`, and the policy-written `uptake.decided` it triggers (actor `policy:<edge>`, refs the publication). The uptake table then shows an auto ripple beside the gated one and the viewer can compare [...]
    verify: The finding's factual claims reproduce exactly, and it is not a restatement of any README boundary. Every binding edge in the fixture is gated, no policy actor ever writes an uptake.decided, and the rendered page shows only "available, gated" -- so on the edge axis the fixture can only [...]
- (medium) [coverage-and-thesis] Supersession (frame Q3, "the design's organizing test") has no fixture instance, and the grid's in-flight arm cannot distinguish it  @ /Users/claire/src/nyobe/delivery-ledger/views.sql:354
    evidence: views.sql:354 `WHEN i.transition IS NOT NULL THEN 'in-flight'` — status is in-flight regardless of whether the in-flight transition's freight matches `d.freight`. `grep -n 'abandon\|supersed' facts.jsonl views.sql render.py` → nothing. thesis-architecture.md:186-188 "Supersession — a newer decision abandons an in-flight transition, newest-wins, incumbent untouched"; :435-437 "Q3 ... this was the design's organizing test." No [...]
    suggestion: Two facts at a fourth instant: a `promotion.decided` for a stage while its transition is in flight (a hotfix cut while T418-staging runs, say) and a `transition.finished` with `outcome: abandoned` whose refs name the superseding decision. Add a `superseded` arm (in-flight AND inflight.freight <> desired) or at minimum a self-check that [...]
    verify: Both factual prongs reproduce, and the gap is not a declared boundary. (1) No fixture instance: a programmatic scan of all 107 facts finds zero `promotion.decided` landing while a service transition on that stage is open, and zero instants (rebuilding the DB at every fact ts) where an in- [...]
- (medium) [coverage-and-thesis] Gate terms are not data the gate reads — the program's structured `requires:` is flattened to text and hand-compiled twice (smells #2 records it; the cheap fix does not need OPEN 4)  @ /Users/claire/src/nyobe/delivery-ledger/views.sql:294
    evidence: smells.md:22-28 already logs the hand-compiled CASE. The second compile: render.py `screen_awaiting` re-lists the same four terms by hand — `terms = [("verified in staging: integration-tests", ...), ("verified in staging: smoke", ...), ("verified in staging: load-generator", ...), ("approved by oncall (merge of the release PR)", ...)]`. program.md:40-44 already has them structured: `requires: [d.verified("staging", "integration-tests"), [...]
    suggestion: Have program apply emit `requires` as a JSON array of typed terms (`{verified:{stage,check}}`, `{approved:{role}}`, `{carried:{stage}}`, `{not_held:{}}`, `{plan_safe_or:{approved:{role}}}`) in `policy.declared`. `v_gate_all`/`v_gate_eval` become a `json_each` over terms joined to `v_verified`/`v_approval`/`v_carried`/`v_hold`, and the [...]
    verify: Not refuted: this is a real gap in the slice as scoped, not a restatement of a declared boundary. README "Boundaries, on purpose" excludes pump/executors/engine changes/ingest/real output records/chrome — none of which is gate-rule representation; gate evaluation at rest (v_gate_eval, beats 2 [...]

=== REFUTED ===

=== LOW (unverified) ===
- [ledger-purity] Out-of-band screen re-evaluates per-service match/differ in Python instead of reading v_observed's derivation @ /Users/claire/src/nyobe/delivery-ledger/render.py:579 :: Add a per-service long-form view (stage, service, desired, observed, matches) over json_each and render it; keep mismatches/mismatch_detail as aggregates over that view so the grid and the side-door screen share [...]
- [ledger-purity] Renderer invents state labels the ledger never carries and un-parses a view-composed string @ /Users/claire/src/nyobe/delivery-ledger/render.py:394 :: Expose these as columns: a v_transition.phase that coalesces to a declared initial phase (or render 'no phase yet' as absence, not a phase); an approval-state column on v_releases if the card wants one; split [...]
- [sql-correctness] v_plan.safe is NULL when `migrations_changed` is absent from the payload; the gate then says 'plan for production-eu' although the plan fact exists @ /Users/claire/src/nyobe/delivery-ledger/views.sql:189 :: Either lint plan.summarized for the keys the safe-rule reads (delete, replace, migrations_changed) or coalesce absent booleans to a deliberate default in v_plan and keep 'plan missing' keyed on plan_fact IS NULL [...]
- [sql-correctness] v_pending_uptake.pending is NULL before any publication and the uptake screen renders it as 'current at v' @ /Users/claire/src/nyobe/delivery-ledger/views.sql:462 :: Distinguish 'nothing published yet' from 'current': `CASE WHEN r.version IS NULL THEN NULL ELSE r.version > coalesce(u.version,0) END` plus a third branch in screen_uptake, or filter edges with no record.
- [sql-correctness] Fixture: F416 never passes through testing, so the 2026-08-19 grid shows testing/testing-eu with no intent or carried freight a day after F416 shipped, and staging's gate for its own carried freight reads 0 @ /Users/claire/src/nyobe/delivery-ledger/facts.jsonl:13 :: Add the F416 testing facts (two promotions, two transitions, one verification) to the fixture, or note the gap in README's 'illustrative' list; as written, the ledger records a staging promotion its own policy [...]
- [scenario-fidelity] README calls click-ops force-deploy 'the emergency path'; the emergency path is update-stack.yml dispatch of any branch, which the fixture doesn't model, and the fixture's rollback crosses a migration without saying so @ /Users/claire/src/nyobe/delivery-ledger/README.md:93 :: Reword README.md:93-95 (click-ops = rollback side door; emergency deploy = update-stack dispatch). Add the migration judgement to f073's rationale ('additive migration, safe to run old code against') — it's the [...]
- [scenario-fidelity] Keith parity table omits what his tracker actually shows inside a deploy (job-step timeline with typical durations, current step) and has no close-release kind or release SHAs @ /Users/claire/src/nyobe/delivery-ledger/README.md:130 :: Add rows for 'in-flight step + ETA' (transition.phase could carry the GHA step name and a typical-duration column derived from prior transitions) and 'release run status/phase'; add a `release.closed` intent kind [...]
- [coverage-and-thesis] The pending-uptake preview is a synthesized string, not a plan fact — the uptake gate cannot be evaluated at rest the way the diff-gate is @ /Users/claire/src/nyobe/delivery-ledger/views.sql:463 :: One `plan.summarized` for the consumer against record v13 (subject shape is the small design call: `stage:production` with `stack: workflow-pool, record_version: 13`, or the edge itself) and a join in [...]
- [coverage-and-thesis] Program declarations that write nothing: `warehouse:`, `verify:`, `enact:` have no declaration facts, and `warehouse:master` is a subject no fact declares @ /Users/claire/src/nyobe/delivery-ledger/program.md:23 :: Either add `warehouse.declared` (intent, subject `warehouse:<name>`, payload repo/branch/images/config source) plus `verification.declared`/`enactment.declared` rows to the table, or state in program.md that those [...]
- [coverage-and-thesis] Wishlist items one field or one fact away: issue/incident and cross-repo linkage on PR entries (Keith #2, #4, #6); front-door rollback and a manual-waiver kind (wishlist #6, #7) @ /Users/claire/src/nyobe/delivery-ledger/facts.jsonl:75 :: Add `closes`/`incident`/`repo` fields to one or two PR entries and a `v_trace` variant keyed on issue; one front-door rollback fact (`promotion.decided` by `user:` to the prior freight, with rationale) in a [...]
- [coverage-and-thesis] `freight.discovered` is correctly observation, but thesis-architecture.md lists "freight declarations" under intent — the reconciliation is owed by the architecture text @ /Users/claire/src/nyobe/claudes-notebook/projects/delivery-as-code/thesis-architecture.md:86 :: Amend the architecture's intent list to "freight nominations (release cuts, pins)" and add "freight discovery" to the observation list, citing the POC; or, if the wording is deliberate, record the deviation in smells.md.

=== COVERED WELL ===
- [ledger-purity]
    · (d) No view reads anything other than facts, clock, another v_ view, or json_each: FROM/JOIN audit of views.sql returned '(none)' outside {facts, clock, json_each, v_*}; [...]
    · (b) No fact payload stores what a view derives: ./render.py lint passes FORBIDDEN_KEYS {status,state} / FORBIDDEN_VALUES (render.py:41-42, 117-121) over all 107 facts, and a [...]
    · Grid status is SQL-only: v_grid's status CASE (views.sql:353-364) is the sole source; screen_grid (render.py:391-396) branches on r['status'] only to pick detail formatting [...]
    · Time-travel holds: build_db (render.py:168-183) filters facts by ts <= as_of and sets clock; 24/24 self-checks pass across four instants (A/B/C/D); screen_outofband correctly [...]
    · SNAPSHOTS captions (render.py:45-50) narrate state in page chrome, but every claim in them is pinned by a self-check at the same as_of (render.py:209-216 for A, 261-263 for [...]
    · Stage-name literals inside views.sql (v_gate_eval CASE at 296-323, v_candidate upstream sentinels at 240-251, v_carried stack='service') are already owned by smells.md:22-42 [...]
- [sql-correctness]
    · Latest-wins dedup (v_stage, v_policy, v_edge, v_desired, v_verified, v_plan, v_observed, v_breakglass, v_record, v_uptaken) is correct for the fixture: no two facts of one [...]
    · as-of 2026-08-24T10:05:02Z (between approval f056 and decision f057): production {desired: F416, carried: F416, candidate: F417, status: 'ready'}, v_gate_eval passes=1, [...]
    · as-of 2026-08-19T09:00:00Z v_pending_uptake: both edges published (v12, v9), nothing consumed → pending 1 with a populated preview; default as-of 13>12 → 1 and 9>9 → 0 as the [...]
    · v_candidate for a stage whose upstream carries nothing: the ELSE branch yields NULL, v_gate_eval's inner JOIN on (stage, freight) drops the stage, and v_grid shows candidate [...]
    · v_grid's break-glass join (views.sql:380, `bg.ts > coalesce(k.since,'')`) attaches f073 (23:40) to production carried since 10:31 at the default instant, producing 'api@F416 [...]
    · ISO-timestamp string comparison (`until > now`, `bg.ts > since`, the max()/ORDER BY ts dedups, and render.py's `f['ts'] <= as_of`) is order-correct because every fixture [...]
    · v_lanes (views.sql:393-394) handles the no-decision case NULL-safely (`d.freight IS NULL OR d.freight <> fr.freight`) — the pattern v_grid should adopt.
    · The proposed NULL-safe patch (coalesce(plan_safe,0) at :307; IS NOT at :355/:356/:358/:360) was run against the full self-check suite: 24/24 still pass, and the three [...]
- [scenario-fidelity]
    · No staging-eu: pr-and-weekly-release-process.md:54 "(There is no `staging-eu`)"; the fixture declares testing, testing-eu, staging, production, production-eu only [...]
    · Five ECS services and their names: rollback-the-pulumi-service.md:15-21 (API, Console, Jobs, Ratelimit, Workflow) match facts.jsonl:74 (f074) `services: {api, console, jobs, [...]
    · Roll durations: emergency-deploy-the-pulumi-service.md:7 "~5 minutes ... then 20-30 minutes to fully roll ECS tasks"; fixture transitions run 17-25 min (f016→f017 [...]
    · Merge as the approval, and what enforces it: pr-staging-deploy.yml:46-58 `staging-checks-pass` (deploy-staging + staging-integration-tests) is the branch-protection gate; [...]
    · Release PR shape matches Keith's definition: model.go:88 "a PR based on `production` labeled `release`"; release.yml:109-111 `--base production --label release`; fixture's [...]
    · Production integration tests and the Slack-on-failure job exist as modelled: push-build.yml:39-45 integration-tests (target-stack production), :77-94 post-errors-to-slack; [...]
    · Migration-safety check described accurately: cmd/prod-rollback/README.md:13-15 (blocks if migrations/ changed; explicit confirmation if gh unavailable) matches README.md:93-94.
    · Cut days are consistent with a weekday cron even though the hour is wrong: F417 discovered Fri 08-21 21:40Z → cut Mon 08-24; F418 discovered Tue 08-25 22:14Z (after the [...]
    · Keith's live view covers all five stacks including production-eu (pulumicloud.go:270-275 DeployStacks), while his release stepper has staging + production only [...]
    · Renderer runs clean: `./render.py --lint-only` was not needed to judge fidelity; the repo was read only, not modified.
- [coverage-and-thesis]
    · All five beats render from views with no side state: grid/lanes (v_grid, v_lanes), awaiting (v_gate_eval — production at Wed 11:45 = `awaiting: 'approval: oncall (merge [...]
    · Class assignments match the architecture: release.cut / promotion.decided / approval.granted / hold.placed / breakglass.recorded / uptake.decided / binding.declared are [...]
    · No awaiting-as-failure anywhere: lint forbids `status`/`state` keys and awaiting/drifted/converged/held/pending/ready/in-flight values; v_releases renders a missing approval [...]
    · Demonstrated from the nine-area wishlist: #3 artifact identity (freight = five image digests × pinned config v41→v42), #8 diff-based gate, #9's consumer-side `upstream` with [...]
    · Ledger-residence criterion is addressable, only unrecorded: 107 facts over 15 days in SQLite (12 declarations, then 8–27 facts per pipeline day) — worth one line in smells.md.
    · Time travel is a real proof, not a UI trick: build_db loads `ts <= as_of` and sets the clock; three snapshots plus the 09:00:30 check exercise it.
